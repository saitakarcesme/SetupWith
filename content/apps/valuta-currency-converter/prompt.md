# Valuta setup prompt

Set up Valuta from its official project and verified Flathub listing (https://apps.gnome.org/Valuta/). Inventory Linux architecture, Flatpak scopes, existing package and data paths, preferred-pair count, cache entry count and age range, provider configuration, locale and decimal conventions, history policy and backup coverage without revealing currencies, amounts, converted values, travel plans, budgets, provider queries or financial context. Install stable `io.github.idevecore.Valuta` from verified Flathub in the existing ownership scope. Do not compile from source, launch Valuta, contact a rate provider, read cached rates, submit an amount, perform or invert a conversion, copy a result, change preferred currencies, clear history, or refresh data during setup.

Preserve preferred pairs, provider choice, cached observations and timestamps, input and display preferences, precision and locale behavior, history, remotes, overrides and scope ownership. Currency amounts and pair choices may reveal travel, salary, purchases or assets; redact them from diagnostics. A displayed conversion is reference information, not an executable foreign-exchange quote. It normally excludes bank spread, card markup, commission, taxes, transfer fees, cash premiums and settlement-time movement; never present it as guaranteed proceeds or financial advice.

Before a later refresh, require the provider's legal identity, HTTPS endpoint, data methodology, source institutions, supported currencies, publication schedule, retention terms and fallback policy. Every result must show base and quote ISO codes, direction, rate, observation timestamp, provider and freshness status. Reject symbol-only ambiguity and silently substituted providers. Define decimal separator, intermediate precision, rounding mode and minor-unit display; retain sufficient precision until the final presentation step. Flag obsolete, redenominated or unavailable currencies, market closures, cross-rate derivation and mixed-timestamp data rather than fabricating continuity.

Set an explicit stale-rate threshold and offline indicator; cache use must retain the original observation time. Verify provenance, app ID, origin, commit, runtime, executable, files and sandbox permissions offline. Prove no DNS request, provider call, cache read or refresh, amount processing, conversion, inversion, clipboard write, preference change, history deletion or notification occurred. On failure, rollback only the new Valuta ref in its original scope; preserve shared runtimes, remotes, overrides, provider settings, preferred pairs, cached observations, timestamps, locale choices, history and backups.
