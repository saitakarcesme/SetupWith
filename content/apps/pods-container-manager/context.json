{
  "platforms": [
    "Linux"
  ],
  "installTime": "5–15 min",
  "complexity": "Advanced",
  "delivery": "store",
  "safety": {
    "requiresSignIn": false,
    "requiresElevation": false,
    "mayInstallDrivers": false,
    "mayInstallKernelComponents": false,
    "mayRequireRestart": false
  },
  "preferences": [
    "engine_connections",
    "registry_policy",
    "runtime_inventory",
    "destructive_controls"
  ],
  "secrets": [],
  "permissionCheckpoints": [
    "Podman or Docker engine type, version, rootless or rootful mode, and socket ownership",
    "local Unix socket or remote TCP endpoint, TLS verification, SSH tunnel, and authentication",
    "containers, pods, images, volumes, networks, ports, health checks, and dependent workloads",
    "registry search, credentials, auth files, image names, tags, digests, signatures, and pulls or pushes",
    "Dockerfile or Containerfile build context, ignore rules, secrets, cache, and generated image",
    "host mounts, devices, capabilities, privileged mode, user namespace, environment, and published ports",
    "logs, processes, terminal exec sessions, clipboard, commands, and sensitive runtime output",
    "bulk start, stop, pause, kill, delete, force removal, image or volume prune, and rollback"
  ]
}
