# Hieroglyphic setup prompt

Set up Hieroglyphic from its official repository and verified Flathub listing (https://apps.gnome.org/Hieroglyphic/). Inventory Linux architecture, Flatpak scopes, existing package and data paths, target typesetting systems, broad engine/package availability, clipboard-manager policy, accelerator availability and accessibility needs without reading documents, sketches or clipboard contents. Install stable `io.github.finefindus.Hieroglyphic` from verified Flathub in the existing ownership scope. Do not compile from source, launch the app, initialize an accelerator, draw a sketch, run recognition, copy a symbol or compile a document during setup.

Preserve preferences, model assets, recent state, clipboard and typesetting projects. Hieroglyphic ranks candidates from a sketch; its top result is a suggestion, not proof of the intended mathematical or textual semantics. Before future insertion show multiple candidates with rendered name, exact LaTeX command or Typst expression, Unicode code point when relevant, math/text mode, required package or import, argument shape and source-model version. Require the author to confirm meaning, especially for visually confusable relations, arrows, Greek letters, combining marks and invisible spacing.

LaTeX and Typst syntax are not interchangeable. Validate the chosen output in the exact engine and project environment using a minimal non-sensitive scratch document, without installing missing packages automatically or enabling shell escape. A command that renders similarly may have different spacing, precedence, accessibility or semantic behavior. Do not rewrite surrounding source, infer imports, replace a Unicode character globally or claim formal correctness from visual appearance.

Clipboard operations can overwrite unrelated data or propagate through synchronized managers. Show the exact non-sensitive output and target before copying, and never inspect or clear pre-existing clipboard content. If NPU acceleration is available, use only an existing trusted driver path after explicit review; do not install drivers for this utility, and retain CPU fallback. Verify provenance, app ID, origin, commit, runtime, executable, bundled recognition model, files and permissions offline. Prove no sketch capture, inference, accelerator access, clipboard change, package install, compilation, network request or filesystem state changed. On failure, rollback only the new Hieroglyphic ref in its original scope; preserve runtimes, remotes, overrides, models, clipboard and documents.
