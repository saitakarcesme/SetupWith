# llama.cpp setup prompt

Codex, inspect the OS, architecture, shell, compiler toolchain, CMake, CPU instruction sets, GPU backend, existing `llama.cpp` checkout or binaries, model files, and occupied server ports. Prefer an official package when maintained for the platform, otherwise clone the canonical `ggml-org/llama.cpp` repository and build in a separate directory with CMake. Select Metal on supported Apple hardware, CUDA or Vulkan only when their toolchains are already valid, and CPU otherwise. Ask before administrator access, installing compilers or GPU toolkits, updating an existing checkout, exposing a server port, changing firewall rules, or downloading models. Preserve local patches, build directories, models, prompts, and scripts. Configure only `model_path`, `context_size`, `gpu_layers`, and `server_port` when supplied. Use `secret://provider/HUGGINGFACE_TOKEN` solely through a secure model-download mechanism; never print, echo, log, or commit it. Validate GGUF compatibility and memory requirements before loading. Verify the built `llama-cli` and `llama-server`, backend detection, model metadata, a short deterministic local completion, and localhost server health. Rollback removes only the new build, binaries, and service entries, retaining models unless deletion is approved. Finish with a structured summary of source revision, build backend, settings, checks, approvals, and rollback.
