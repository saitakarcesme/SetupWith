# Video Trimmer setup prompt

Set up Video Trimmer from its official GNOME project and verified Flathub listing (https://apps.gnome.org/VideoTrimmer/). Inventory Linux architecture, Flatpak scopes, existing package and data paths, recent-state and partial-output byte counts, portal grants, output policy, available media runtimes and backup coverage without opening videos or revealing filenames, paths, thumbnails, durations, timestamps, codecs, subtitles, audio, metadata or content. Install stable `org.gnome.gitlab.YaLTeR.VideoTrimmer` from verified Flathub in the existing ownership scope. Do not compile from source, launch the app, select a file, parse or decode media, generate a thumbnail, play audio, choose trim points, invoke a codec, create an output, or modify recent state during setup.

Preserve every source byte, owner, permission and useful timestamp, plus existing outputs, partial files, recent state, portal grants, remotes, overrides and scope ownership. Video and audio may be confidential, and filenames or thumbnails can expose their subject; redact them from diagnostics. “No re-encoding” preserves encoded samples but does not guarantee frame-exact boundaries, universal playback or identical container metadata. Keyframe placement, timestamp edit lists and player behavior can expose earlier frames or shift the visible start.

Before a later trim, require an input checksum, detected container and stream manifest, duration and time base, requested start and end with inclusivity, variable-frame-rate handling and keyframe-boundary policy. Declare which video, audio, subtitle, chapter and attachment streams survive, along with language/default flags and metadata. Preview only with explicit approval, safe decoder limits, confirmed muted output and no automatic codec installation. Show output path, free-space estimate and collision decision; write atomically, never overwrite the source, retain partials only by policy, and verify the result in more than one relevant player when boundary accuracy matters.

Verify provenance, app ID, origin, commit, runtime, executable, files and sandbox permissions offline. Confirm file-portal reach without selecting media. Prove no file open, media parse, decoder, thumbnail, audio, timestamp selection, stream copy, temporary output, collision, recent-file mutation or notification occurred. On failure, rollback only the new Video Trimmer ref in its original scope; preserve shared runtimes, remotes, overrides, source media, outputs, metadata, portal grants and backups.
