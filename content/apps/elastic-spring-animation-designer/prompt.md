# Elastic setup prompt

Set up Elastic from its official project and verified Flathub listing (https://apps.gnome.org/Elastic/). Inventory Linux architecture, Flatpak scopes, existing package and data paths, saved-preset count, target libadwaita and language-binding choices, parameter-range and reduced-motion policy presence, export-state count, clipboard and portal grants, sandbox overrides and backup coverage without revealing project names, animation values, generated code, destinations, copied snippets or private interface designs. Install stable `app.drey.Elastic` from verified Flathub in the existing ownership scope. Do not compile from source, launch Elastic, restore a preset, start a preview, move the interactive handle, sample a curve, calculate duration, change spring or transform values, generate code, read or write the clipboard, create a file or execute a snippet during setup.

Preserve every saved spring and transform parameter, target language and binding version, graph preference, reduced-motion setting, generated snippet, window state, permission, remote, override and scope ownership. Installation must not reinterpret units, clamp parameters, resample curves, replace a preset, start motion or insert generated code into a project. Generated output is version-bound source text, not a universally correct animation. Redact project context.

Before later design, pin the libadwaita API and binding version and document each spring parameter's unit, valid range and default. Define translation coordinates, rotation pivot, scale origin, composition order and exact equilibrium. Report overshoot, oscillation and settling duration with the numerical tolerance and sampling resolution used; a plotted approximation must not be confused with runtime timing. Validate representative parameter boundaries against the target library and keep a deterministic reference set.

Every preview needs a reduced-motion alternative, bounded translation and rotation, immediate stop, and no hazardous flashing caused by scale or repeated oscillation. Export requires an explicit C, JavaScript, Python, Vala or Rust target, imports, numeric precision and ownership rules. Preview the complete snippet and destination diff; never auto-paste, overwrite project code, invoke a compiler or run the animation. Verify provenance, app ID, origin, commit, runtime, executable, files and permissions offline. Prove no preset read, preview, handle movement, graph sampling, duration calculation, parameter change, code generation, clipboard access, file write, compiler or execution occurred. On failure, rollback only the new Elastic ref; preserve shared runtimes, presets, target versions, accessibility choices, snippets and backups.
