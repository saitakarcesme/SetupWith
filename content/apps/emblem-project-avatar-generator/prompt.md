# Emblem setup prompt

Set up Emblem from its official GNOME project and verified Flathub listing (https://apps.gnome.org/Emblem/). Inventory Linux architecture, Flatpak scopes, existing package and data paths, symbolic-icon catalog count by package class, saved-design and export-state counts, destination portal grants, network permission presence, sandbox overrides and backup coverage without revealing project, Matrix room or forge names, selected icons, colors, generated avatars, filenames, destinations, account identities or unpublished branding. Install stable `org.gnome.design.Emblem` from verified Flathub in the existing ownership scope. Do not compile from source, launch Emblem, enumerate private asset folders, select or render an icon, restore a design, calculate a palette, generate an avatar, open a portal, create or overwrite a file, access Matrix or a forge, upload an image or change any remote avatar during setup.

Preserve every symbolic source asset, saved design, palette, geometry, export preference, existing avatar file, portal grant, permission, remote, override and scope ownership. A generated avatar can represent a community or software project; installation must not select a default symbol, imitate another identity, replace existing branding, strip attribution or imply project approval. Redact branding under development. Emblem generates assets locally; publishing to Matrix rooms or Git forges is a separate authorized action.

Before later generation, record the symbolic icon's source package, version, checksum, author, license, modification rights and trademark review. Confirm the intended project identity and compare against existing rooms and forge namespaces to reduce impersonation or collision risk. Define foreground and background colors, contrast thresholds, light and dark presentation, monochrome fallback and color-vision accessibility. Keep the important mark inside both square and circular safe areas and test legibility at the smallest deployed size and high-density scales.

Preview every output format and dimension actually offered by the installed version, including transparency, color profile, metadata and deterministic checksum. Use a canonical filename, reject symlink and normalization collisions, preserve an existing asset by default and publish through an atomic local write. Test with a synthetic public icon and temporary destination. Never open a browser, authenticate, invalidate remote caches or upload automatically. Verify provenance, app ID, origin, commit, runtime, executable, files and permissions offline. Prove no asset enumeration, icon selection, render, palette calculation, avatar generation, portal, file write, network, upload or remote mutation occurred. On failure, rollback only the new Emblem ref; preserve shared runtimes, source icons, designs, palettes, avatar files, permissions and backups.
