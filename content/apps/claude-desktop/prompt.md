# Claude Desktop setup prompt

Set up Claude Desktop from its verified official source (https://claude.com/download). Supported targets: macOS, Windows. Begin with a read-only preflight covering the operating system, architecture, shell, package managers, disk space, relevant runtimes or hardware, and any existing installation. Download Claude Desktop only from Anthropic's official download page, select the matching macOS or Windows build, validate its signature, and keep any previous installation intact.

Preserve Preserve existing Claude Desktop data, MCP configuration, connector definitions, downloaded artifacts, keyboard shortcuts, and any enterprise-managed settings or policies.. Back up files before editing and explain the selected version, source, location, dependencies, and commands. Apply only supplied, supported preferences: launch_on_startup, desktop_notifications, global_shortcut, local_integrations. No credential references are needed for this setup.

Ask before elevation, system-wide packages, services or startup items, ports or firewall rules, sign-in, large downloads, overwrites, or migrations.  Do not enable startup items, background services, overlays, notifications, analytics, or device permissions unless they are requested and separately approved. Do not sign in, import MCP servers, enable filesystem connectors, approve extension access, or grant accessibility and screen permissions until the user reviews the exact data boundary. For a large game or media download, report the exact size, destination drive, free space, and bandwidth impact, then wait for approval. Never delete game libraries, save files, media libraries, mods, profiles, or cloud-sync data. If the platform or configuration is unsupported, stop and offer the closest official alternative.

Open Claude Desktop without authenticating, record the version and executable location, confirm the expected Anthropic publisher, and verify that no connector or startup item was enabled automatically. Report the version, executable, configuration and data paths, changed files, active processes or listeners, and verification evidence without exposing sensitive values. End with manual steps and exact rollback or uninstall instructions that remove only setup-owned changes, restore backups, stop new services, and preserve user data.
