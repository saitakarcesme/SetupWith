# Transformers setup prompt

Set up Transformers from its verified official source (https://github.com/huggingface/transformers). Supported targets: macOS, Linux, Windows. Begin with a read-only preflight covering the operating system, architecture, shell, package managers, disk space, relevant runtimes or hardware, and any existing installation. Create or reuse an approved isolated Python environment, choose PyTorch, TensorFlow, or Flax support deliberately, and install a stable Transformers release compatible with that backend.

Preserve Python environments, dependency lockfiles, Hugging Face cache directories, offline settings, local models, and existing authentication. Back up files before editing and explain the selected version, source, location, dependencies, and commands. Apply only supplied, supported preferences: python_version, virtual_environment, model_cache_directory, compute_backend. The only available credential references are secret://hf/token. Treat every reference as opaque: never print, echo, log, resolve, or paste its value.

Ask before elevation, system-wide packages, services or startup items, ports or firewall rules, sign-in, large downloads, overwrites, or migrations.  Do not enable startup items, background services, overlays, notifications, analytics, or device permissions unless they are requested and separately approved. Do not authenticate, download models, trust remote model code, change shared caches, or enable GPU packages without approval. For a large game or media download, report the exact size, destination drive, free space, and bandwidth impact, then wait for approval. Never delete game libraries, save files, media libraries, mods, profiles, or cloud-sync data. If the platform or configuration is unsupported, stop and offer the closest official alternative.

Import `transformers`, print only its version and backend availability, and instantiate a local configuration object without contacting the Hub or downloading weights. Report the version, executable, configuration and data paths, changed files, active processes or listeners, and verification evidence without exposing sensitive values. End with manual steps and exact rollback or uninstall instructions that remove only setup-owned changes, restore backups, stop new services, and preserve user data.
