# Codex CLI setup prompt

Set up Codex CLI from its verified official source (https://developers.openai.com/codex/cli/). Supported targets: macOS, Linux, Windows. Begin with a read-only preflight covering the operating system, architecture, shell, package managers, disk space, relevant runtimes or hardware, and any existing installation. Follow the current OpenAI Codex CLI page for the detected platform, prefer its standalone installer or supported package route, and avoid replacing a pinned installation without approval.

Preserve Preserve existing Codex configuration, authentication state, AGENTS.md files, skills, plugins, MCP definitions, shell completions, session history, and repository-specific instructions.. Back up files before editing and explain the selected version, source, location, dependencies, and commands. Apply only supplied, supported preferences: model, reasoning_effort, approval_policy, sandbox_mode. The only available credential references are secret://openai/api-key. Treat every reference as opaque: never print, echo, log, resolve, or paste its value.

Ask before elevation, system-wide packages, services or startup items, ports or firewall rules, sign-in, large downloads, overwrites, or migrations.  Do not enable startup items, background services, overlays, notifications, analytics, or device permissions unless they are requested and separately approved. Do not authenticate, insert an API key, weaken sandbox or approval settings, install plugins, register MCP servers, modify shell startup files, or open a repository until explicitly approved. For a large game or media download, report the exact size, destination drive, free space, and bandwidth impact, then wait for approval. Never delete game libraries, save files, media libraries, mods, profiles, or cloud-sync data. If the platform or configuration is unsupported, stop and offer the closest official alternative.

Run `codex --version` and a help-only command outside private repositories, confirm the resolved executable and configuration paths, and verify that no login or project session was created. Report the version, executable, configuration and data paths, changed files, active processes or listeners, and verification evidence without exposing sensitive values. End with manual steps and exact rollback or uninstall instructions that remove only setup-owned changes, restore backups, stop new services, and preserve user data.
