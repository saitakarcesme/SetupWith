# Parabolic setup prompt

Set up Parabolic from the official Nickvision repository and current Flathub listing (https://flathub.org/apps/org.nickvision.tubeconverter). Note that the product is now named Parabolic while its stable app ID remains `org.nickvision.tubeconverter`. Inventory Linux architecture, Flatpak scopes, existing ref and data ownership, queue/history counts, destination grant, browser-integration and cookie-setting presence, bundled yt-dlp/ffmpeg classes, concurrent-job limit, sandbox overrides and backup coverage without reading clipboard contents, URLs, titles, account names, cookies, download history or filenames. Match the manifest to official upstream before installing the stable ref in the existing scope. Do not launch Parabolic, inspect the clipboard, import browser cookies, resolve a URL, contact a site, enumerate a playlist, create a destination, download or post-process anything during setup.

Preserve queue, history, archive, preferences, partial files, destinations, cookies, browser profiles, permissions, remotes, overrides and ownership. URLs may contain tokens and reveal private interests; redact query strings, fragments, titles and paths. Never assume that technical accessibility grants permission. Require publisher, ownership or license, site terms, consent, age and regional review before every later download. Do not bypass DRM, authentication, paywalls, rate limits or access controls.

Before a job, show the canonical redacted URL, redirect boundary, single-item or playlist scope, item count, live status, time range, formats, resolution, codecs, subtitles, metadata, thumbnail, chapters, estimated bytes and post-processing. Browser cookies are optional secrets: identify the minimum profile and domain scope, never print or copy values, obtain separate approval, and provide revocation. Validate destination containment, Unicode-normalized filenames, symlinks, collisions, quotas and free space; keep partial files distinguishable and never overwrite silently. Bound concurrency and bandwidth on metered networks.

Verify repository ownership, product/app-ID mapping, manifest, commit, runtime, executable, yt-dlp/ffmpeg versions, files, checksums and permissions offline. Prove no clipboard read, cookie import, URL resolution, network request, playlist expansion, file creation, download or post-processing occurred. On failure, rollback only the new Parabolic ref; preserve runtimes, queues, history, archives, cookies, browser data, partials, destinations, remotes, overrides and backups.
