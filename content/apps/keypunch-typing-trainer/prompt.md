# Keypunch setup prompt

Set up Keypunch from its official repository and verified Flathub listing (https://apps.gnome.org/Keypunch/). Inventory Linux architecture, Flatpak scopes, existing package and data paths, keyboard layout and input-method names, available practice languages, accessibility needs, result-history presence and custom-text count without recording keystrokes, reading custom text, querying clipboard contents or exposing performance metrics. Install stable `no.bragefuglseth.Keypunch` from verified Flathub in the existing ownership scope. Do not compile from source, launch the app, start a session, capture input, import custom text, reset results or change keyboard settings during setup.

Preserve session results, preferences, selected language/layout, custom exercises and application data. Custom text may contain private correspondence, copyrighted prose, source code, tokens or customer identifiers; require ownership and a content scan before import, use a protected local copy, and never source practice material from password managers, terminals, production logs or clipboard history. Do not print custom content or individual keystrokes in diagnostics. Keep network access and telemetry absent during typing sessions unless separately justified.

Match practice language to the physical layout, input method and intended skill. Generated C++, JavaScript, Python or Rust text exercises practice character sequences, not program correctness; do not execute or save them into a codebase. Review generated corpora for unsuitable or offensive terms before supervised educational use. Ensure focus remains inside the practice field and stop a session before switching to authentication or messaging windows so accidental secrets are not captured as errors.

WPM and accuracy vary with language, punctuation, session length, warm-up and input method; never compare unlike sessions or use results for employment, grading or medical claims. Set conservative duration and breaks, preserve system accessibility, and stop for pain, numbness or fatigue. Verify Keypunch provenance, app ID, origin, commit, runtime, executable, corpus assets, files and permissions offline. Prove no keystroke hook, custom-text read, clipboard access, session, result, history, keyboard configuration, network request or filesystem state changed. On failure, rollback only the new Keypunch ref in its original scope; preserve runtimes, remotes, overrides, exercises and metrics.
