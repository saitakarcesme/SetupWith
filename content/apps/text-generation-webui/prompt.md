# Text generation web UI setup prompt

Set up Text generation web UI from its verified official source (https://github.com/oobabooga/text-generation-webui). Supported targets: macOS, Linux, Windows. Begin with a read-only preflight covering the operating system, architecture, shell, package managers, disk space, relevant runtimes or hardware, and any existing installation. Use the project's documented one-click installer or a dedicated environment that matches the detected CPU and accelerator; review installer scripts and select a stable release before execution.

Preserve model directories, characters, presets, extensions, training data, command flags, installer files, and existing Python or Conda environments. Back up files before editing and explain the selected version, source, location, dependencies, and commands. Apply only supplied, supported preferences: model_cache_directory, compute_backend, listen_address, listen_port, default_model. The only available credential references are secret://hf/token. Treat every reference as opaque: never print, echo, log, resolve, or paste its value.

Ask before elevation, system-wide packages, services or startup items, ports or firewall rules, sign-in, large downloads, overwrites, or migrations.  Do not enable startup items, background services, overlays, notifications, analytics, or device permissions unless they are requested and separately approved. Do not download models, trust remote code, install third-party extensions, expose the web UI beyond loopback, or enable its API without approval. For a large game or media download, report the exact size, destination drive, free space, and bandwidth impact, then wait for approval. Never delete game libraries, save files, media libraries, mods, profiles, or cloud-sync data. If the platform or configuration is unsupported, stop and offer the closest official alternative.

Verify the launcher and environment versions, then start the UI on a loopback-only address with no model loaded and confirm the local health page before stopping it. Report the version, executable, configuration and data paths, changed files, active processes or listeners, and verification evidence without exposing sensitive values. End with manual steps and exact rollback or uninstall instructions that remove only setup-owned changes, restore backups, stop new services, and preserve user data.
