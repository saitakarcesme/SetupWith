# Wike setup prompt

Set up Wike from its official project and verified Flathub listing (https://apps.gnome.org/Wike/). Inventory Linux architecture, Flatpak scopes, existing package and data paths, configured language count, bookmark-list and history counts, open-tab restoration state, aggregate cache bytes, desktop search-provider state, personal-data-on-exit policy, sandbox overrides and backup coverage without revealing queries, article titles, bookmarks, tabs, languages, reading interests or cached content. Install stable `com.github.hugolabe.Wike` from verified Flathub in the existing ownership scope. Do not compile from source, launch Wike, contact Wikipedia, issue a search, fetch or render an article, load media, expose suggestions, restore tabs, read history, follow a link, or clear personal data during setup.

Preserve language choices, bookmark lists, history, open tabs and restoration behavior, cached content, text and display preferences, desktop search settings, permissions, remotes, overrides and scope ownership. Search and reading history may reveal medical, political, religious, legal or personal concerns; redact it from diagnostics. Desktop search integration can surface sensitive titles outside Wike, including during screen sharing. Installation must not infer a preferred language from private history, merge bookmarks across language editions, restore an old sensitive session or clear data on exit.

Before later network use, require the exact Wikipedia language edition and Wikimedia API origin, redirect policy, user-agent and cache behavior. Different language editions are independently edited and may disagree; show edition, revision ID and retrieval timestamp. Wikipedia is a starting point, not an authoritative source for medical, legal, financial or safety-critical decisions; preserve citations and article warnings. Treat article HTML, templates, images, media, math, maps and external links as untrusted remote content. Restrict automatic loads to reviewed Wikimedia origins, preview external final hosts and never open downloads or URI handlers automatically.

Review Wike's declared home-directory and portal access against actual needs and remove unexplained overrides rather than browsing files. Verify provenance, app ID, origin, commit, runtime, executable, files and static sandbox permissions offline. Prove no DNS request, API call, search, suggestion, article or media load, tab restoration, history or bookmark read, external link, file access, cache mutation or personal-data clearing occurred. On failure, rollback only the new Wike ref in its original scope; preserve shared runtimes, remotes, overrides, languages, bookmarks, history, tabs, caches, privacy settings and backups.
