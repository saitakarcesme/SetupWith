# FL Studio setup prompt

Set up FL Studio from its verified official source (https://www.image-line.com/fl-studio/download). Supported targets: macOS, Windows. Begin with a read-only preflight covering the operating system, architecture, shell, package managers, disk space, relevant runtimes or hardware, and any existing installation. Use Image-Line's single official FL Studio installer for the supported Windows or macOS architecture, recognize that edition features unlock through the account, and retain the current installation when updating over it. Keep optional FL Cloud content and large sample downloads out of scope.

Preserve FLP projects, recorded audio, sliced data, User data folder, browser favorites, templates, presets, scores, backups, plug-in database, VST search paths, custom samples, MIDI scripts, audio settings, license state, and previous installer channel. Back up files before editing and explain the selected version, source, location, dependencies, and commands. Apply only supplied, supported preferences: user_data_folder, audio_driver, plugin_search_paths, autosave_frequency. No credential references are needed for this setup.

Ask before elevation, system-wide packages, services or startup items, ports or firewall rules, sign-in, large downloads, overwrites, or migrations. Browser Use and Computer Use may open the official page, download the approved signed installer, complete the visible installer flow, verify the publisher or code signature, and launch the product once. Stop and hand control to the user before account creation, sign-in, password entry, MFA, CAPTCHA, age verification, purchases, subscriptions, license acceptance that creates a paid obligation, or linking external accounts. Never bypass DRM, regional restrictions, or platform protections. Do not enable startup items, background services, overlays, notifications, analytics, or device permissions unless they are requested and separately approved. Do not sign in or unlock an edition, scan VST folders, relocate User data, install third-party plug-ins, download FL Cloud sounds, change ASIO or CoreAudio devices, open a project, render audio, or place personal files inside the replaceable macOS application bundle without approval. For a large game or media download, report the exact size, destination drive, free space, and bandwidth impact, then wait for approval. Never delete game libraries, save files, media libraries, mods, profiles, or cloud-sync data. If the platform or configuration is unsupported, stop and offer the closest official alternative.

Open FL Studio to an empty default project with audio output muted, report the exact build and trial or unlocked state without account data, inspect User data and backup paths, and confirm no plug-in scan ran. Rollback retains projects, recordings, custom samples, database, and license files. Report the version, executable, configuration and data paths, changed files, active processes or listeners, and verification evidence without exposing sensitive values. End with manual steps and exact rollback or uninstall instructions that remove only setup-owned changes, restore backups, stop new services, and preserve user data.
