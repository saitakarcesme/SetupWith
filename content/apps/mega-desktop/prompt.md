# MEGA Desktop setup prompt

Set up MEGA Desktop from its verified official source (https://mega.io/desktop). Supported targets: macOS, Linux, Windows. Begin with a read-only preflight covering the operating system, architecture, shell, package managers, disk space, relevant runtimes or hardware, and any existing installation. Select the stable MEGA Desktop package for the exact OS, architecture, and Linux distribution from MEGA's official page. Install the synchronization client only; do not add optional browser integrations or create any sync or backup pair during setup.

Preserve existing MEGA sync and backup pairs, exclusion rules, transfer queues, local databases, cached thumbnails, bandwidth schedules, recovery-key records, unsynced changes, and folders previously designated as computer backups. Back up files before editing and explain the selected version, source, location, dependencies, and commands. Apply only supplied, supported preferences: sync_pairs, backup_folders, excluded_patterns, bandwidth_limit. No credential references are needed for this setup.

Ask before elevation, system-wide packages, services or startup items, ports or firewall rules, sign-in, large downloads, overwrites, or migrations. Browser Use and Computer Use may open the official page, download the approved signed installer, complete the visible installer flow, verify the publisher or code signature, and launch the product once. Stop and hand control to the user before account creation, sign-in, password entry, MFA, CAPTCHA, age verification, purchases, subscriptions, license acceptance that creates a paid obligation, or linking external accounts. Never bypass DRM, regional restrictions, or platform protections. Do not enable startup items, background services, overlays, notifications, analytics, or device permissions unless they are requested and separately approved. Stop before MEGA account sign-in, two-factor verification, recovery-key handling, choosing local or cloud folders, starting a backup, importing links, downloading cloud content, resolving conflicts, enabling full-sync, or changing bandwidth and startup behavior. For a large game or media download, report the exact size, destination drive, free space, and bandwidth impact, then wait for approval. Never delete game libraries, save files, media libraries, mods, profiles, or cloud-sync data. If the platform or configuration is unsupported, stop and offer the closest official alternative.

Confirm the signed MEGA binary and version, open the client only to its welcome screen, and inspect processes without authenticating. Verify there are no new sync pairs, backup jobs, transfers, watched folders, or autostart entries; rollback must preserve the local MEGA database and queued work. Report the version, executable, configuration and data paths, changed files, active processes or listeners, and verification evidence without exposing sensitive values. End with manual steps and exact rollback or uninstall instructions that remove only setup-owned changes, restore backups, stop new services, and preserve user data.
