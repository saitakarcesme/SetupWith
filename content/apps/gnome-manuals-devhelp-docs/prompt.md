# Manuals setup prompt

Set up Manuals from its official GNOME project and verified Flathub listing (https://apps.gnome.org/Manuals/). Inventory Linux architecture, Flatpak scopes, existing package and data paths, discovered SDK and Devhelp-book counts by version class, aggregate index and cache bytes, bookmark and history counts, filesystem grants, network permission presence, sandbox overrides and backup coverage without reading documentation content or revealing manual titles, symbol names, search queries, viewed APIs, project paths, usernames, source links or inferred development work. Install stable `org.gnome.Manuals` from verified Flathub in the existing ownership scope. Do not compile from source, launch Manuals, scan SDK roots, enumerate books, parse Devhelp or HTML files, build an index, search, restore history, render a page, follow a link, open a browser, access a portal or contact a network during setup.

Preserve every documentation source byte, package and version association, existing index, cache, bookmark, history, display preference, permission, remote, override and scope ownership. Developer searches and viewed symbols can reveal confidential projects or vulnerabilities; redact them from diagnostics. Installation must not merge books from incompatible SDK versions, prefer an unversioned manual over a pinned runtime, remove an orphaned index, traverse symlinks beyond approved roots or make Manuals the default documentation handler without review.

Before a later index build, require each Devhelp root's owner, SDK or runtime ID, architecture, package version, checksum and license. Parse manifests and HTML as untrusted local content with file, markup-depth, image, font, memory and time limits; disable active content and nonessential URI schemes. Resolve duplicate namespaces and cross-references by explicit SDK version rather than newest-looking filenames. Build deterministically into a new destination, cap disk use and atomically swap only after book, chapter, symbol and broken-link counts pass. Test with a disposable synthetic manual, not proprietary documentation.

Define local search-history retention and bookmark behavior. Label the exact SDK and package version beside results so outdated APIs are not presented as current; documentation is version-specific and may contain errors. External source and web links require final-host preview and confirmation, without carrying local queries or file paths. Verify provenance, app ID, origin, commit, runtime, executable, files and static permissions offline. Prove no root scan, book enumeration, parser, index write, search, history or bookmark read, page render, link activation, portal or network request occurred. On failure, rollback only the new Manuals ref in its original scope; preserve shared runtimes, remotes, overrides, documentation, indexes, histories, bookmarks, settings and backups.
