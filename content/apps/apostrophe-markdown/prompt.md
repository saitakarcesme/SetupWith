# Apostrophe setup prompt

Set up Apostrophe from its official GNOME project and verified Flathub listing (https://apps.gnome.org/Apostrophe/). Inventory Linux architecture, Flatpak scopes, existing Apostrophe package, document locations, recovery state, recent-file entries, Markdown flavor, spellchecking languages, preview preferences, Pandoc availability, export templates, TeX Live extension and file associations without reading document contents. Install stable `org.gnome.gitlab.somas.Apostrophe` from verified Flathub in the existing ownership scope. Do not replace an unofficial distro build, compile from source, launch the editor, open a document or install the optional TeX Live extension.

Preserve every Markdown file, unsaved recovery artifact, encoding, newline style, metadata block, relative link, image, template, export and preference byte-for-byte. Do not create a sample document, trigger autosave, normalize whitespace, change Markdown flavor, claim file associations or clear recent files. Documents may contain private drafts, credentials, legal text or unpublished research; verify paths and hashes only, and never include text, headings, statistics or spellcheck findings in diagnostics.

Live preview renders Markdown through WebKit. Treat raw HTML, embedded images, styles, links, data URIs and local resource references as untrusted document content. Do not render a file, fetch remote assets, follow links, expose adjacent local files or allow a preview to establish network state. Before future preview use show the document trust boundary, Markdown flavor, raw-HTML policy, allowed resource roots and external endpoint behavior.

Pandoc export can transform content into PDF, Word/LibreOffice, LaTeX, HTML or reveal.js slides and may read templates, filters, fonts and linked resources. Require explicit input, output format, destination, overwrite behavior, resource paths and expected fidelity changes. Never install TeX Live, run LaTeX, enable shell escape, invoke filters or overwrite an export as a setup test. Verify Apostrophe source, app ID, origin, commit, runtime, executable, files and permissions offline. Prove no document, recovery file, recent entry, preview process, network request, export, temporary file, extension or association changed. On failure, rollback only the new Apostrophe ref in its original scope; preserve shared runtimes, remotes, overrides, documents, recovery data and export toolchains.
