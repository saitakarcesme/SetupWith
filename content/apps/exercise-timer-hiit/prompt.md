# Exercise Timer setup prompt

Set up Exercise Timer from its official GNOME project and verified Flathub listing (https://apps.gnome.org/Hiit/). Inventory Linux architecture, Flatpak scopes, existing package and data paths, saved-preset count, audio server, selected output, notification policy, display sleep behavior and accessibility needs without reading preset names or inferring health information. Install stable `xyz.safeworlds.hiit` from verified Flathub in the existing ownership scope. Do not compile from source, launch the app, create a workout, play a beep, change audio routing or begin a timer during setup.

Preserve every preset name, set count, exercise/rest/preparation duration, ordering, volume preference and theme setting byte-for-byte. Routine names can reveal health goals or schedules; exclude them from diagnostics. Do not overwrite defaults, normalize durations, create a demonstration HIIT plan or delete an apparently unused preset. Exercise Timer measures intervals only: it does not identify an appropriate exercise, assess fitness, monitor vital signs or provide medical clearance.

Before any future session show the approved activity, participant, environment, equipment, preparation time, work/rest ratio, set count, total duration, cue volume, output device, pause/cancel method and explicit stop conditions. Require conservative progression for an unfamiliar routine. Stop for pain, dizziness, breathing difficulty or loss of coordination and seek appropriate help; a timer completion must never be treated as evidence that exertion was safe. Keep emergency communication accessible and do not let screen or audio cues distract from surroundings.

Transition beeps can be startling or unsafe through headphones and may route to a shared or Bluetooth speaker. Test only after separate approval, begin at low volume and confirm the physical destination. Preserve system volume, limiter, focus, notifications and power settings. Verify Exercise Timer provenance, app ID, origin, commit, runtime, executable, files and declared permissions offline. Prove no audio stream, route, preset, timer, notification, inhibitor, display setting or filesystem state changed. On failure, rollback only the new Exercise Timer ref in its original scope; preserve shared runtimes, remotes, overrides, presets and desktop audio configuration.
