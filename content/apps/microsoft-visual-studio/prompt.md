# Microsoft Visual Studio setup prompt

Set up Microsoft Visual Studio from its verified official source (https://visualstudio.microsoft.com/downloads/). Supported targets: Windows. Begin with a read-only preflight covering the operating system, architecture, shell, package managers, disk space, relevant runtimes or hardware, and any existing installation. Acquire the official Visual Studio bootstrapper from Microsoft, verify its signature, identify the requested edition, and present workload sizes and component paths before installing anything.

Preserve Preserve installed Visual Studio instances, workloads, SDKs, extensions, settings, licenses, source caches, offline layouts, custom toolsets, and existing installer configuration.. Back up files before editing and explain the selected version, source, location, dependencies, and commands. Apply only supplied, supported preferences: edition, install_location, selected_workloads, component_cache_location. No credential references are needed for this setup.

Ask before elevation, system-wide packages, services or startup items, ports or firewall rules, sign-in, large downloads, overwrites, or migrations.  Treat drivers, kernel components, anti-cheat, background services, overlays, firewall changes, startup items, and restarts as separate approval checkpoints. Explain the exact component, vendor, privileges, persistence, and rollback before continuing. Require approval for every workload, Windows SDK, compiler, emulator, database component, driver, service, file association, elevation request, license or trial, and restart. For a large game or media download, report the exact size, destination drive, free space, and bandwidth impact, then wait for approval. Never delete game libraries, save files, media libraries, mods, profiles, or cloud-sync data. If the platform or configuration is unsupported, stop and offer the closest official alternative.

Use Visual Studio Installer to confirm the exact instance and workload state, launch the IDE without a solution, report the build, and verify no unapproved optional component was installed. Report the version, executable, configuration and data paths, changed files, active processes or listeners, and verification evidence without exposing sensitive values. End with manual steps and exact rollback or uninstall instructions that remove only setup-owned changes, restore backups, stop new services, and preserve user data.
