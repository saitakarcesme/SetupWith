# Ear Tag setup prompt

Set up Ear Tag from its official project and verified Flathub listing (https://apps.gnome.org/EarTag/). Inventory Linux architecture, Flatpak scopes, existing package and data paths, recent-file count, supported container classes, pending batch and rename-state presence, aggregate artwork and cache bytes, network permission, sandbox overrides and backup coverage without opening audio or revealing filenames, tags, artwork, acoustic fingerprints, listening collections, performers, albums, comments, paths or private recordings. Install stable `app.drey.EarTag` from verified Flathub in the existing ownership scope. Do not compile from source, launch Ear Tag, enumerate a folder, open or parse an audio file, decode artwork, calculate a fingerprint, contact AcoustID, fetch metadata, select a candidate, alter tags, rename a file, create a backup or update recents during setup.

Preserve every audio frame, container structure, known and unknown tag, embedded picture, chapter, replay-gain field, timestamp, owner, permission and filename, plus pending edits, preferences, remotes, overrides and scope ownership. Installation must not build a music library, normalize Unicode, discard custom fields, rewrite containers, apply downloaded metadata, rename tracks or infer that files in one directory form an album. Audio and tag content is private; redact it.

Before a later edit, identify the exact container and tag implementation and create a per-file field-level diff. In a bulk selection, distinguish common, mixed, cleared and untouched values; never propagate the first file's value implicitly. Validate track and disc numbers without flattening multi-disc structure. Embedded artwork is untrusted image input and needs signature, MIME, dimensions, byte and pixel limits plus license review. Preserve audio-frame checksums whenever the container permits and document unavoidable byte changes. Test with synthetic files covering every supported format and unknown fields.

For tag-driven renaming, preview every final basename and canonical path, sanitize separators and reserved names, detect normalization, case-only and many-to-one collisions, and use a reversible two-phase rename. AcoustID identification requires explicit selected-file consent and disclosure of fingerprint and metadata transmission; pin the endpoint and show candidate confidence and provenance. A match is a suggestion, never permission to overwrite. Verify provenance, app ID, origin, commit, runtime, executable, files and permissions offline, proving no file open, parser, artwork decode, fingerprint, network request, metadata fetch, tag write, rename, backup or recent mutation occurred. On failure, rollback only the new Ear Tag ref; preserve shared runtimes, originals, tags, filenames, pending state and backups.
