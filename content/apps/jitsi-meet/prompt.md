# Jitsi Meet setup prompt

Set up Jitsi Meet from its verified official source (https://github.com/jitsi/jitsi-meet). Supported targets: Linux. Begin with a read-only preflight covering the operating system, architecture, shell, package managers, disk space, relevant runtimes or hardware, and any existing installation. Use Jitsi's documented Debian or Docker self-hosting path on an approved Linux host, with a real domain and TLS plan required before any non-local deployment.

Preserve Jitsi Meet configuration, Prosody data, authentication secrets, recordings, certificates, web customizations, and container volumes. Back up files before editing and explain the selected version, source, location, dependencies, and commands. Apply only supplied, supported preferences: deployment_mode, domain_name, data_directory, http_port, https_port. The only available credential references are secret://jitsi/jwt-secret. Treat every reference as opaque: never print, echo, log, resolve, or paste its value.

Ask before elevation, system-wide packages, services or startup items, ports or firewall rules, sign-in, large downloads, overwrites, or migrations. Browser Use and Computer Use may open the official page, download the approved signed installer, complete the visible installer flow, verify the publisher or code signature, and launch the product once. Stop and hand control to the user before account creation, sign-in, password entry, MFA, CAPTCHA, age verification, purchases, subscriptions, license acceptance that creates a paid obligation, or linking external accounts. Never bypass DRM, regional restrictions, or platform protections. Do not enable startup items, background services, overlays, notifications, analytics, or device permissions unless they are requested and separately approved. Do not configure DNS, request certificates, expose ports, create authentication secrets, join or create meetings, or enable recording without approval. For a large game or media download, report the exact size, destination drive, free space, and bandwidth impact, then wait for approval. Never delete game libraries, save files, media libraries, mods, profiles, or cloud-sync data. If the platform or configuration is unsupported, stop and offer the closest official alternative.

Validate configuration and container definitions; for an approved local test, confirm services become healthy on loopback and stop them without opening a meeting room. Report the version, executable, configuration and data paths, changed files, active processes or listeners, and verification evidence without exposing sensitive values. End with manual steps and exact rollback or uninstall instructions that remove only setup-owned changes, restore backups, stop new services, and preserve user data.
