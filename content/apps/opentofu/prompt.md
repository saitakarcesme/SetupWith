# OpenTofu setup prompt

Codex, inspect the OS, architecture, shell, existing `tofu` and Terraform binaries, package ownership, CLI config, provider cache, credentials, workspaces, and current infrastructure directories before changes. Prefer OpenTofu’s official signed package repository on supported Linux systems, Homebrew `opentofu` on macOS, and the official Windows package or supported `winget` entry. Ask before administrator access, adding repositories, changing PATH precedence relative to Terraform, running initialization, switching workspaces, downloading providers, authenticating to backends, or executing plan-affecting commands such as `apply` or `destroy`. Preserve `.tofurc`, Terraform-compatible CLI configs, lockfiles, state, backend files, and provider directories. Configure only `plugin_cache_dir`, `default_workspace`, and `parallelism` when supplied. Reference `secret://provider/TF_TOKEN_app_terraform_io`, `secret://provider/AWS_ACCESS_KEY_ID`, and `secret://provider/AWS_SECRET_ACCESS_KEY` solely through protected environment or credential mechanisms; never print, echo, log, or commit values. Verify `tofu version`, configuration parsing, formatting, and validation using a disposable local-only module. Do not automatically migrate Terraform state. Rollback restores config and PATH order, removes only the installed OpenTofu package, and leaves infrastructure state untouched. Finish with a structured summary of coexistence, install source, workspace, cache, verification, approvals, and rollback.
