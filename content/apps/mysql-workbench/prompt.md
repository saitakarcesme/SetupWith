# MySQL Workbench setup prompt

Set up MySQL Workbench from its verified official source (https://dev.mysql.com/downloads/workbench/). Supported targets: macOS, Linux, Windows. Begin with a read-only preflight covering the operating system, architecture, shell, package managers, disk space, relevant runtimes or hardware, and any existing installation. Select the MySQL Workbench Community package for the detected operating system from Oracle's official download page, validate its signature, and review prerequisites before installation.

Preserve Preserve saved connections, password-vault entries, SSH configuration, SQL history, snippets, models, migration projects, plugins, server profiles, and safe-update preferences.. Back up files before editing and explain the selected version, source, location, dependencies, and commands. Apply only supplied, supported preferences: connection_storage, sql_safe_updates, ssh_configuration, model_directory. No credential references are needed for this setup.

Ask before elevation, system-wide packages, services or startup items, ports or firewall rules, sign-in, large downloads, overwrites, or migrations.  Do not enable startup items, background services, overlays, notifications, analytics, or device permissions unless they are requested and separately approved. Do not sign in to Oracle, install MySQL Server, add connectors, import credentials, open an SSH tunnel, disable safe updates, alter a server, or run SQL without approval. For a large game or media download, report the exact size, destination drive, free space, and bandwidth impact, then wait for approval. Never delete game libraries, save files, media libraries, mods, profiles, or cloud-sync data. If the platform or configuration is unsupported, stop and offer the closest official alternative.

Launch Workbench to its home screen, report the exact build and configuration directory, and confirm no connection, migration wizard, server action, or SQL statement was initiated. Report the version, executable, configuration and data paths, changed files, active processes or listeners, and verification evidence without exposing sensitive values. End with manual steps and exact rollback or uninstall instructions that remove only setup-owned changes, restore backups, stop new services, and preserve user data.
