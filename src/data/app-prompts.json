{
  "visual-studio-code": "Codex, first inspect the operating system, CPU architecture, current shell, PATH, existing Visual Studio Code installations, and any managed or portable setup. Report what you find before changing anything. On macOS, prefer the official signed app or Homebrew Cask package `visual-studio-code`; on Debian/Ubuntu use Microsoft’s signed repository and `code` package; on Fedora-family systems use Microsoft’s RPM repository; on Windows prefer `winget install Microsoft.VisualStudioCode`, unless an existing installer channel should be preserved. Ask before using administrator access, replacing an existing app bundle, changing PATH, or enabling browser sign-in. Preserve `settings.json`, `keybindings.json`, snippets, profiles, and extensions. Configure only `preferred_shell`, `editor_font_size`, and `editor_theme` when supplied, merging settings rather than overwriting files. If requested, install the `code` shell command through the supported mechanism and verify it resolves correctly. Confirm launchability, print `code --version`, open a temporary folder, and check that the integrated terminal uses the requested shell without modifying the system default. Rollback must remove only components added in this run and restore any backed-up settings. Finish with a structured summary containing detected platform, install method, changed files, configured values, verification results, skipped approvals, and rollback commands.",
  "git": "Codex, begin by detecting the OS, architecture, login shell, PATH order, existing Git binaries, package-manager ownership, and current global and system Git configuration. Do not replace a vendor-provided Git or alter repositories until you explain the detected state. On macOS prefer Xcode Command Line Tools when already present, otherwise Homebrew `git`; on Debian/Ubuntu use `apt install git`; on Fedora-family systems use `dnf install git`; on Windows prefer `winget install Git.Git`. Ask before administrator access, package upgrades, changing credential helpers, or editing system-wide configuration. Preserve all existing `.gitconfig` files and includes. Configure only `git_name`, `git_email`, `default_branch`, and `preferred_editor` when provided, using scoped `git config --global` updates and making a timestamped backup first. Do not create commits or modify an existing repository. Verify with `git --version`, `git config --show-origin --list`, and a disposable temporary repository that tests initialization, the requested default branch, and local commit metadata without contacting a remote. If rollback is requested, restore the backed-up global config and uninstall only the package installed during this run. End with a structured summary of detected binaries, chosen package source, config keys changed, test repository results, approvals declined or pending, and exact rollback steps.",
  "github-cli": "Codex, inspect the OS, architecture, shell, PATH, existing `gh` binary, Git installation, credential helpers, and current GitHub CLI authentication status before making changes. On macOS use Homebrew package `gh`; on Debian/Ubuntu use GitHub CLI’s signed apt repository and package `gh`; on Fedora-family systems use the official RPM repository; on Windows prefer `winget install GitHub.cli`. Ask before administrator access, adding repositories, changing Git protocol defaults, opening a browser, or initiating sign-in. Preserve `~/.config/gh/config.yml`, `hosts.yml`, and existing Git settings. Configure only `git_protocol` and `default_editor` when supplied, merging through `gh config set` without exposing credentials. If `secret://provider/GITHUB_TOKEN` is available, use it through a non-printing authentication path; never echo, log, persist in shell history, or display the value. Otherwise, ask before browser/device authentication. Verify with `gh --version`, `gh config list`, `gh auth status` when authenticated, and a read-only API command such as viewing the current user or a public repository. Do not create issues, repositories, or pull requests. Rollback should restore backed-up config and remove only the installed package. Finish with a structured summary covering installation, authentication method, config changes, verification, approvals, and rollback commands.",
  "neovim": "Codex, first inspect the operating system, CPU architecture, shell, terminal capabilities, existing `nvim` binary, package-manager ownership, and current Neovim configuration directories. On macOS prefer Homebrew `neovim`; on Debian/Ubuntu use the distribution package unless it is clearly too old for the user’s existing configuration, then explain safer alternatives; on Fedora-family systems use `dnf install neovim`; on Windows prefer `winget install Neovim.Neovim`. Ask before administrator access, replacing another editor command, installing fonts, or modifying shell aliases. Preserve `~/.config/nvim`, `init.lua`, `init.vim`, plugin lockfiles, and data directories. Configure only `preferred_shell`, `editor_theme`, and `leader_key` when provided. Merge settings into a clearly marked SetupWith block or separate Lua module, and never overwrite an existing configuration. Do not install a plugin manager unless requested; if a theme requires a plugin, ask first and document it. Verify using `nvim --version`, `nvim --headless '+checkhealth' +qa`, and a temporary file opened in headless mode. Confirm the requested shell option and leader mapping without launching destructive plugin updates. Rollback must remove the marked block or added module and uninstall only this run’s package. End with a structured summary of paths, changes, health results, skipped items, and rollback steps.",
  "zed": "Codex, inspect the OS, architecture, current shell, GPU/display environment, existing Zed installation, application data, and whether the platform is officially supported before proceeding. Install only on supported systems. On macOS prefer the official signed Zed application or Homebrew Cask `zed`; on supported Linux use Zed’s official installation path; do not invent a native Windows installation if unavailable. Ask before administrator access, replacing an app bundle, adding the `zed` CLI to PATH, opening a browser, or enabling account synchronization. Preserve Zed settings, keymaps, extensions, and workspace state. Configure only `editor_theme`, `editor_font_size`, and `preferred_shell` when supplied, merging into the existing settings file without deleting unrelated keys. If AI providers are requested, reference only `secret://provider/ANTHROPIC_API_KEY` or `secret://provider/OPENAI_API_KEY`; never print, echo, log, or hard-code secret values. Ask before writing provider credentials into any application-managed credential store. Verify the installed application launches, the `zed` command resolves when enabled, settings parse correctly, and a temporary folder opens with the requested terminal shell. Avoid account sign-in during verification. Roll back by restoring backed-up settings, removing added CLI links, and uninstalling only the installed channel. Finish with a structured summary of support status, install source, config changes, verification, approvals, and rollback.",
  "sublime-text": "Codex, start by detecting the OS, architecture, login shell, existing Sublime Text installations, package source, executable locations, and current user configuration. On macOS prefer the official signed application or Homebrew Cask `sublime-text`; on Debian/Ubuntu use Sublime HQ’s signed apt repository and package `sublime-text`; on Fedora-family systems use the official RPM repository; on Windows prefer `winget install SublimeHQ.SublimeText.4`. Ask before administrator access, adding repositories, replacing an app bundle, registering file associations, or creating PATH links. Preserve the `Packages/User` directory, installed packages, license state, session data, and existing `Preferences.sublime-settings`. Configure only `editor_theme`, `editor_font_size`, and `preferred_shell` when provided. Merge JSON safely, retain comments where possible, and add terminal integration only if an existing package supports it; do not install Package Control or third-party plugins without approval. For shell access, use the supported `subl` command and ask before creating symlinks. Verify the application binary, `subl --version` where supported, JSON validity, launch of a temporary text file, and persistence of the selected theme and font size. Rollback should restore the backed-up User settings, remove added links, and uninstall only this run’s package. End with a structured summary of installation, files changed, tests, approvals, and rollback commands.",
  "helix": "Codex, inspect the operating system, architecture, shell, terminal, existing `hx` binary, package ownership, and Helix runtime/config paths before installing anything. On macOS prefer Homebrew package `helix`; on Debian/Ubuntu use the available `helix` package when suitable or the project’s official release artifact after explaining the choice; on Fedora-family systems prefer the native package; on Windows use `winget install Helix.Helix` when available. Ask before administrator access, downloading a release archive, replacing an existing binary, or modifying PATH. Preserve `~/.config/helix/config.toml`, `languages.toml`, runtime files, and user themes. Configure only `editor_theme`, `preferred_shell`, and `clipboard_provider` when supplied. Merge valid TOML without removing unrelated keys. Set shell commands in the Helix configuration only when supported, and verify the selected clipboard provider exists rather than silently installing additional utilities. Run `hx --version`, `hx --health`, and a non-destructive open test against a temporary file. Check that runtime queries load, the configured theme resolves, and clipboard integration reports usable status. Do not install language servers automatically unless separately approved. Rollback must restore the original TOML files, remove any PATH entry added during this run, and uninstall only the chosen package. Finish with a structured summary of detection, install source, configuration, health checks, skipped dependencies, and rollback.",
  "emacs": "Codex, first inspect the OS, architecture, shell, graphical session, existing Emacs binaries, package-manager source, and user configuration such as `~/.emacs`, `~/.emacs.d`, or `~/.config/emacs`. On macOS prefer a maintained Homebrew Emacs package or the official GNU build appropriate to the user’s existing channel; on Debian/Ubuntu use `apt install emacs`; on Fedora-family systems use `dnf install emacs`; on Windows prefer `winget install GNU.Emacs`. Ask before administrator access, changing the active Emacs distribution, installing fonts, or replacing file associations. Preserve all init files, package archives, lockfiles, custom variables, and server configuration. Configure only `editor_theme`, `preferred_shell`, and `font_size` when supplied. Add an isolated, clearly marked SetupWith Lisp block or separate file loaded from the existing init, avoiding duplicate `custom-set-variables` sections. Do not refresh package archives or install themes without approval. Verify with `emacs --version`, a batch-mode load of the user configuration, and `emacs --batch --eval` checks for the requested shell and font settings where applicable. Launching the GUI requires approval in remote environments. Rollback should remove the added block or file, restore backups, and uninstall only the installed package. Finish with a structured summary of detected init path, installation method, configuration edits, batch results, approvals, and rollback.",
  "tmux": "Codex, inspect the OS, architecture, login shell, terminal type, existing `tmux` binary, server sessions, and configuration files before making changes. On macOS use Homebrew `tmux`; on Debian/Ubuntu use `apt install tmux`; on Fedora-family systems use `dnf install tmux`; on Windows do not claim native support—use an existing WSL environment only after confirming the user wants that. Ask before administrator access, terminating sessions, changing the default shell, or replacing terminal key bindings. Preserve `~/.tmux.conf`, included files, plugins, and all running sessions. Configure only `prefix_key`, `default_shell`, and `mouse_support` when provided. Validate the requested prefix syntax, ensure the shell executable exists, and append a clearly marked SetupWith section rather than overwriting existing directives. Do not install TPM or plugins unless approved. Verify with `tmux -V`, `tmux source-file` against a temporary combined config, and a detached test server using a unique socket name. Confirm the prefix binding, shell command, and mouse option through `show-options`; do not attach to or alter existing sessions. Rollback removes the marked section, restores the backup, and uninstalls only the package added here. End with a structured summary of existing sessions, install method, config changes, test results, approvals, and rollback commands.",
  "starship": "Codex, first detect the operating system, architecture, active and login shells, terminal, existing `starship` binary, package ownership, and current shell startup files. On macOS prefer Homebrew `starship`; on Debian/Ubuntu or Fedora-family systems use an available trusted package or Starship’s official installer after explaining the source; on Windows prefer `winget install Starship.Starship`. Ask before administrator access, executing a downloaded installer, modifying shell startup files, or installing a Nerd Font. Preserve existing `starship.toml` and shell initialization. Configure only `preferred_shell`, `prompt_preset`, and `nerd_font` when supplied. Initialize Starship in the correct profile using an idempotent, clearly marked line, and generate or merge the selected built-in preset without overwriting unrelated modules. Treat `nerd_font` as a preference check: verify availability and ask before installing any font. Verify with `starship --version`, `starship explain`, `starship timings`, and a clean non-interactive shell invocation that confirms initialization without duplicate prompt hooks. Avoid changing the system default shell. Rollback should remove the marked initialization line, restore the previous config, and uninstall only the package added during this run. Finish with a structured summary of shell detection, install source, files changed, preset status, font status, verification, approvals, and rollback.",
  "homebrew": "Codex, inspect the OS, CPU architecture, shell, existing `brew` commands, installation prefixes, ownership, and shell environment before proceeding. Homebrew is appropriate only for macOS and supported Linux environments; do not attempt installation on Windows outside an explicitly approved WSL setup. If Homebrew already exists, diagnose it before upgrading or repairing. For a new installation, use the official Homebrew installer from the canonical repository, but ask before administrator access, creating `/opt/homebrew`, `/usr/local`, or Linuxbrew directories, changing ownership, or editing shell profiles. Preserve existing taps, formulae, casks, services, and configuration. Configure only `install_prefix` and `auto_update` when supplied. Never force a nonstandard prefix unless Homebrew supports it and the user explicitly approves. Add `brew shellenv` to the correct profile through a clearly marked, idempotent block. Interpret `auto_update` through supported Homebrew environment variables, without disabling security updates silently. Verify using `brew --version`, `brew config`, `brew doctor`, prefix ownership checks, and a read-only `brew update --preinstall` only with approval. Do not install arbitrary formulae as a test. Rollback should remove only profile lines and files created in this run; full Homebrew removal requires separate confirmation because it is destructive. End with a structured summary of prefix, architecture, profile edits, diagnostic results, approvals, and safe rollback options.",
  "nix": "Codex, begin by inspecting the OS, architecture, shell, existing Nix installation, daemon state, profiles, channels or flakes, and `/nix` ownership. Support macOS and Linux only; do not invent a native Windows setup. Prefer the official Nix installer and determine whether single-user or multi-user mode fits the platform. Ask before administrator access, creating or modifying `/nix`, installing or changing the Nix daemon, editing shell profiles, enabling experimental features, or adding trusted users. Preserve `/etc/nix/nix.conf`, user profiles, channels, flake registries, and existing store data. Configure only `nix_channel`, `experimental_features`, and `trusted_users` when supplied. Merge settings carefully, deduplicate values, and never broaden trusted users without explicit approval. If flakes are requested, enable only the necessary supported feature flags. Verify with `nix --version`, `nix show-config`, `nix-store --verify --check-contents` only if reasonable, and a harmless evaluation or temporary shell such as `nix-shell -p hello --run hello` after explaining any download. Check daemon health where applicable without restarting it unless approved. Rollback should restore configuration backups and remove profile hooks; uninstalling Nix or deleting `/nix` requires separate destructive confirmation. Finish with a structured summary of install mode, daemon status, config changes, verification, approvals, and rollback boundaries.",
  "mise": "Codex, inspect the operating system, architecture, shell, PATH, existing `mise`, `asdf`, runtime shims, and shell startup files before installation. On macOS prefer Homebrew `mise`; on Linux use a trusted distribution package when current or mise’s official installer after explaining the source; on Windows prefer `winget install jdx.mise`. Ask before administrator access, running a downloaded installer, changing PATH order, replacing existing version-manager hooks, or modifying project files. Preserve all existing `.mise.toml`, `mise.toml`, `.tool-versions`, and shell configuration. Configure only `default_runtimes`, `activate_shell`, and `global_config` when supplied. If activation is requested, add the shell-specific `mise activate` command in a marked, idempotent block. Store global runtime selections in the user config, but do not install large runtimes or migrate from asdf without approval. Never rewrite project-local version files automatically. Verify with `mise --version`, `mise doctor`, `mise config ls`, and `mise ls`. If approved runtimes are installed, confirm their executables resolve through a clean shell and report shim precedence. Avoid modifying the system default runtime. Rollback removes the activation block, restores config backups, and uninstalls only mise; runtime caches remain unless separately approved for deletion. Finish with a structured summary of detected managers, installation, config, runtime actions, verification, approvals, and rollback.",
  "nodejs": "Codex, first inspect the OS, architecture, shell, PATH, existing `node`, `npm`, Corepack, version managers, and project version files. Do not replace a managed Node installation without explaining ownership. Prefer an existing mise, nvm, fnm, or Volta workflow when present; otherwise use the platform’s trusted package manager, such as Homebrew on macOS, an official NodeSource channel or distribution package on Linux, and `winget install OpenJS.NodeJS.LTS` on Windows. Ask before administrator access, adding repositories, changing PATH, or selecting a different major release channel. Preserve npm configuration, global packages, Corepack settings, and project lockfiles. Configure only `node_version`, `package_manager`, and `npm_registry` when supplied. Pin the runtime through the detected version manager where possible. Enable Corepack only for supported package managers and never regenerate lockfiles. If authentication is required, use only `secret://provider/NPM_TOKEN` through the package manager’s secure configuration path; never print, echo, log, or commit it. Verify `node --version`, `npm --version`, the selected package manager, registry reachability, and a temporary script execution. Do not run project installs unless requested. Rollback restores config backups, removes only the installed runtime version or package, and leaves project files untouched. Finish with a structured summary of runtime source, PATH resolution, config, verification, approvals, and rollback.",
  "deno": "Codex, inspect the operating system, architecture, shell, PATH, existing `deno` installation, package-manager ownership, and any project `deno.json` files before changing anything. On macOS prefer Homebrew `deno`; on Linux use the official Deno installer or an appropriate trusted package after explaining the choice; on Windows prefer `winget install DenoLand.Deno`. Ask before administrator access, executing a remote installer, editing PATH, changing file associations, or granting runtime permissions. Preserve existing Deno configuration, caches, lockfiles, tasks, and shell completion setup. Configure only `deno_version`, `default_permissions`, and `npm_registry` when provided. Pin versions through the existing manager when possible. Treat `default_permissions` conservatively: do not create blanket `--allow-all` aliases; instead document or create narrowly scoped task flags only after approval. If private registries are needed, reference `secret://provider/DENO_AUTH_TOKENS` without printing, echoing, logging, or committing the secret. Verify with `deno --version`, `deno info`, a temporary `deno eval`, and a permission-denied test demonstrating the security model. Validate registry configuration without downloading project dependencies unless requested. Rollback restores profile and config backups, removes added completions, and uninstalls only the installed Deno channel. End with a structured summary of install method, version management, permissions, registry configuration, verification, approvals, and rollback steps.",
  "bun": "Codex, begin by detecting the OS, architecture, shell, PATH, existing `bun` binary, package-manager ownership, and project files such as `bun.lock`, `package.json`, or runtime version declarations. Install only on supported platforms. On macOS and Linux prefer Bun’s official installer unless Homebrew already manages the installation; on Windows use the official supported installation method or `winget` when available. Ask before administrator access, running a downloaded installer, editing PATH, replacing Node tooling, or modifying project lockfiles. Preserve Bun configuration, caches, registries, global packages, and all existing lockfiles. Configure only `bun_version`, `npm_registry`, and `install_cache_dir` when supplied. Use the existing version manager if it already controls Bun. For private registry access, reference only `secret://provider/NPM_TOKEN`; never print, echo, log, or store the value in committed files. Do not run `bun install` inside a user project without approval. Verify with `bun --version`, `bun pm cache`, a temporary `bun -e` script, and creation plus execution of a disposable test outside existing repositories. Confirm registry and cache settings without publishing anything. Rollback restores shell and config backups, removes only the installed Bun version or package, and leaves project artifacts untouched. Finish with a structured summary of detected tooling, install source, configured values, verification, approvals, and rollback.",
  "python": "Codex, inspect the OS, architecture, shell, PATH, existing `python`, `python3`, pip, virtual environments, package managers, and project version files before installation. Never replace the operating system’s managed Python. Prefer an existing mise or pyenv workflow; otherwise use Homebrew on macOS, distribution packages such as `python3`, `python3-pip`, and `python3-venv` on Linux, and `winget install Python.Python.3` on Windows. Ask before administrator access, changing PATH or file associations, compiling Python, or modifying system certificates. Preserve pip configuration, virtual environments, user packages, and project lockfiles. Configure only `python_version`, `virtualenv_location`, and `package_index` when supplied. Pin the interpreter through the detected version manager where possible and create no project environment unless requested. For package indexes, write only non-secret URLs to supported config; do not embed credentials. Verify interpreter resolution with `python3 --version` or the intended `python --version`, `python -m pip --version`, `python -m venv` in a temporary directory, and a small standard-library script. Do not globally upgrade pip or install demonstration packages without approval. Rollback restores profile and pip config backups, removes only the installed interpreter or version-manager entry, and preserves user environments. End with a structured summary of interpreter ownership, install method, configured values, verification, approvals, and rollback.",
  "uv": "Codex, first inspect the OS, architecture, shell, PATH, existing `uv` binary, Python installations, project files, and any package-index configuration. On macOS prefer Homebrew `uv` when Homebrew manages developer tools, otherwise Astral’s official installer; on Linux use the official installer or trusted distribution package; on Windows prefer `winget install astral-sh.uv`. Ask before administrator access, executing a downloaded installer, editing PATH, changing Python selections, or modifying project lockfiles. Preserve `uv.toml`, `pyproject.toml`, `uv.lock`, pip configuration, caches, and virtual environments. Configure only `python_version`, `package_index`, and `cache_dir` when supplied. Use `uv python pin` only in a user-approved project; otherwise set user-level preferences without creating files in repositories. If authenticated index access is required, reference `secret://provider/UV_INDEX_TOKEN` through a non-printing environment or credential mechanism, never echoing, logging, or committing it. Verify with `uv --version`, `uv self version`, `uv python list`, and a disposable temporary project using `uv venv` plus a standard-library Python command. Avoid resolving or changing an existing lockfile. Rollback restores config and profile backups, removes the installed uv package or binary, and keeps caches unless deletion is approved. Finish with a structured summary of installation, Python selection, index and cache settings, verification, approvals, and rollback.",
  "rust": "Codex, inspect the OS, architecture, shell, PATH, existing `rustc`, `cargo`, rustup, system packages, installed toolchains, targets, and project `rust-toolchain` files before changing anything. Prefer rustup for user-managed Rust on macOS, Linux, and Windows; if a package manager already owns Rust, explain the conflict rather than layering installations. Ask before administrator access, executing the rustup installer, changing PATH, removing distribution packages, or downloading additional targets and components. Preserve `~/.rustup`, `~/.cargo`, Cargo configuration, credentials, installed binaries, and project files. Configure only `rust_toolchain`, `cargo_install_root`, and `default_target` when supplied. Use rustup overrides only where explicitly requested, and do not overwrite project-specific toolchain declarations. Set Cargo’s install root through supported environment or config mechanisms while preserving existing values. Verify with `rustc --version`, `cargo --version`, `rustup show`, and a disposable `cargo new` project built and tested outside existing repositories. If a non-host target is configured, verify installation but do not cross-compile unless the required linker exists. Rollback restores shell and Cargo config backups, removes only newly installed toolchains or the rustup installation, and preserves user crates unless approved. Finish with a structured summary of toolchain ownership, selected target, config changes, test results, approvals, and rollback.",
  "go": "Codex, begin by detecting the OS, architecture, shell, PATH, existing `go` binary, package-manager ownership, `GOTOOLCHAIN`, workspaces, and project `go.mod` files. Prefer an existing mise or version manager when present; otherwise use Homebrew `go` on macOS, the trusted distribution package or official Go archive on Linux after explaining freshness and provenance, and `winget install GoLang.Go` on Windows. Ask before administrator access, replacing an existing toolchain, editing PATH, or downloading a different toolchain automatically. Preserve `go env` settings, module caches, workspaces, credentials, and project files. Configure only `go_version`, `go_workspace`, and `go_proxy` when supplied. Use `go env -w` only after backing up current values, and never set `GOPATH` unnecessarily. For private modules, reference `secret://provider/GOPRIVATE` only as a configuration alias; never print, echo, log, or place secret material in source files. Verify with `go version`, `go env`, `go tool dist list`, and a disposable temporary module that runs `go test` using only standard-library code. Do not download dependencies from an existing project. Rollback restores previous `go env` values and profile files, removes only the installed toolchain, and leaves module caches unless approved. Finish with a structured summary of install source, workspace and proxy settings, verification, approvals, and rollback.",
  "ollama": "Codex, first inspect the operating system, CPU architecture, active shell, available RAM, GPU vendor, existing `ollama` binary, model store, and any running Ollama process or service. Use only supported platforms. On macOS or Windows prefer Ollama’s official signed application installer; on Linux use the official install path or architecture-specific package documented by Ollama. Ask before administrator access, replacing binaries, creating or changing a system service, opening port 11434, altering firewall rules, or downloading large models. Preserve existing models, manifests, logs, and configuration. Configure only `model_names`, `model_storage_path`, `listen_address`, and `gpu_backend` when supplied. Validate storage capacity and GPU compatibility before changing paths or acceleration settings. Bind to localhost by default; require explicit approval for network exposure. Pull only approved model names and show expected download sizes when discoverable. Verify with `ollama --version`, `ollama list`, service health, and a local API check against `/api/tags`; run a brief prompt only with an already installed or approved model. Rollback must restore configuration, stop and remove only services created here, and uninstall without deleting models unless separately approved. Finish with a structured summary covering platform, install method, service state, configured values, models, verification, approvals, and rollback commands.",
  "open-webui": "Codex, inspect the OS, architecture, shell, existing Open WebUI deployment, Docker or Podman availability, Python environment, occupied ports, persistent volumes, and any connected Ollama endpoint before changing anything. Prefer the official container image and a named persistent volume; use the documented Python package only when containers are unsuitable and the existing environment supports it. On Windows, use Docker Desktop or an already configured WSL container workflow rather than inventing a native desktop installation. Ask before administrator access, installing a container engine, starting daemons, binding ports, changing firewall rules, opening a browser, or replacing containers and volumes. Preserve the Open WebUI data volume and existing environment files. Configure only `ollama_base_url`, `webui_port`, and `default_models` when supplied. Reference `secret://provider/OPENAI_API_KEY` and `secret://provider/WEBUI_SECRET_KEY` only through secure runtime injection; never print, log, or store their values in committed files. Bind locally unless external access is approved. Verify container health, persistent volume mounting at `/app/backend/data`, the HTTP readiness endpoint, Ollama connectivity, and that configured models appear without sending private prompts. Rollback should stop and remove only the created container while retaining data unless deletion is approved. End with a structured summary of runtime, image, port, volume, integrations, checks, approvals, and rollback.",
  "localai": "Codex, begin by detecting the operating system, architecture, shell, CPU instruction support, GPU hardware, existing LocalAI installation, container engine, occupied ports, and model directories. Prefer LocalAI’s official Docker or Podman image across supported platforms; on macOS, the official application package is also acceptable when a desktop setup is requested. Use documented Linux binaries only when containers are unavailable. Ask before administrator access, installing or starting a container daemon, exposing ports, changing firewall rules, downloading models, or overwriting model configuration. Preserve existing model files, galleries, container volumes, and YAML configuration. Configure only `model_gallery`, `context_size`, `threads`, and `listen_address` when provided. Use `secret://provider/LOCALAI_API_KEY` and `secret://provider/HUGGINGFACE_TOKEN` solely through secure environment injection; never print, echo, log, or commit them. Default the API to localhost and require approval for remote binding. Select a small compatible model only after checking disk, RAM, and acceleration support. Verify the container or binary version, health endpoint, `/v1/models`, and one short OpenAI-compatible completion using an approved local model. Rollback should remove only resources created here and retain model storage unless deletion is authorized. Finish with a structured summary of backend, hardware path, model source, settings, verification, approvals, and rollback.",
  "comfyui": "Codex, first inspect the OS, architecture, shell, GPU vendor and memory, Python installation, existing ComfyUI or Comfy Desktop setup, custom nodes, models, workflows, and occupied ports. On supported Windows and macOS systems prefer the official ComfyUI Desktop installer; on Linux use the official manual installation with Git and a dedicated Python virtual environment, selecting the correct PyTorch backend for detected hardware. Ask before administrator access, installing GPU drivers, opening ports, changing firewall rules, downloading checkpoints, replacing environments, or overwriting workflows. Preserve `custom_nodes`, `models`, `input`, `output`, user settings, and workflow JSON files. Configure only `model_path`, `output_path`, `listen_address`, and `gpu_backend` when supplied. Reference `secret://provider/HUGGINGFACE_TOKEN` only through a secure downloader or environment mechanism; never print or log it. Bind to loopback unless LAN access is explicitly approved. Do not install third-party custom nodes automatically. Verify startup logs, GPU detection, HTTP access, model path discovery, output write permissions, and execution of a minimal built-in workflow only when a compatible approved model exists. Rollback restores paths and settings, removes only the created environment or application, and retains models and outputs unless approved. End with a structured summary of installation, acceleration, paths, service state, checks, approvals, and rollback.",
  "stable-diffusion-webui": "Codex, inspect the OS, architecture, shell, GPU hardware, Python and Git versions, existing AUTOMATIC1111 Stable Diffusion WebUI checkout, launch scripts, extensions, models, outputs, and port usage before making changes. Use the project’s official Git repository and platform launch script: `webui-user.bat` on supported Windows setups or `webui.sh` on supported macOS/Linux systems, inside a dedicated directory and virtual environment. Ask before administrator access, installing GPU drivers or system packages, downloading checkpoints, exposing port 7860, modifying firewall rules, updating the repository, or overwriting launch configuration. Preserve models, embeddings, extensions, `config.json`, styles, outputs, and existing command-line arguments. Configure only `model_path`, `output_path`, `listen_address`, and `commandline_args` when supplied. Reference `secret://provider/HUGGINGFACE_TOKEN` only through secure download tooling; never print, echo, log, or commit it. Default to local binding and reject unsafe flags unless explicitly justified. Verify repository origin, environment creation, startup completion, HTTP availability, model discovery, and a lightweight generation only with an approved compatible checkpoint. Rollback restores launch files, removes only the new checkout or environment, and keeps model/output directories unless deletion is approved. Finish with a structured summary of backend, paths, flags, verification, approvals, and rollback.",
  "invokeai": "Codex, begin by inspecting the OS, architecture, shell, GPU and available memory, Python environment, existing InvokeAI installation, model cache, database, output directory, and occupied ports. Use InvokeAI’s official installer or documented package workflow for the detected supported platform; prefer a dedicated environment and avoid mixing with unrelated Python packages. Ask before administrator access, installing GPU drivers, downloading models, migrating the database, opening a port, changing firewall rules, launching a browser, or replacing configuration. Preserve the InvokeAI root directory, model records, workflows, board data, database, and generated images. Configure only `model_path`, `output_path`, and `listen_address` when provided. Use `secret://provider/HUGGINGFACE_TOKEN` only via a secure environment or credential mechanism and never print, echo, log, or write its value into shared configuration. Bind locally unless external access is explicitly approved. Do not import or convert models without confirming disk requirements and licenses. Verify the InvokeAI command, application startup, database health, HTTP interface, model path recognition, and output permissions; perform a minimal generation only if an approved compatible model already exists. Rollback restores backed-up configuration and database, removes only the installed environment, and retains models and outputs unless deletion is authorized. End with a structured summary of installer, acceleration, paths, migrations, checks, approvals, and rollback.",
  "jan": "Codex, inspect the operating system, architecture, shell, GPU capabilities, existing Jan application, data directory, downloaded models, local API settings, and occupied ports before installation. Use Jan’s official signed desktop release for supported macOS, Windows, or Linux platforms; prefer the platform-native package format and verify publisher or checksum information when available. Ask before administrator access, replacing an application, adding startup behavior, opening the local API port, changing firewall rules, browser sign-in, or downloading large models. Preserve Jan’s settings, conversations, extensions, model directory, and inference configuration. Configure only `model_directory`, `inference_backend`, and `api_port` when supplied. Reference `secret://provider/OPENAI_API_KEY` only through Jan’s secure credential storage after approval; never print, echo, log, or place its value in plaintext configuration. Keep the API bound to localhost unless remote access is explicitly approved. Select an inference backend only after checking hardware support. Verify application launch, model directory access, backend detection, local API health, and a short local chat using an existing or approved model. Rollback should restore settings, remove only the installed application and API startup changes, and retain conversations and models unless deletion is approved. Finish with a structured summary of package source, backend, data paths, API state, verification, approvals, and rollback.",
  "gpt4all": "Codex, first inspect the OS, architecture, shell, CPU features, GPU availability, existing GPT4All desktop or SDK installation, model directory, settings, chats, and local API port usage. Install the official GPT4All desktop application using its signed native package for the detected supported platform; do not substitute unrelated Python packages. Ask before administrator access, replacing an application, enabling launch at login, opening ports, changing firewall rules, or downloading model files. Preserve existing chats, model metadata, settings, and local documents used for retrieval. Configure only `model_directory`, `default_model`, and `device` when supplied. Validate the requested device against detected hardware and fall back only after explaining the limitation. Before selecting or downloading a default model, present its source, license, approximate storage requirement, and compatibility when available. Keep any local API bound to loopback unless exposure is approved. Verify application launch, model directory permissions, device detection, model integrity, and a short offline response using an existing or approved model; confirm no cloud provider is contacted during that test. Rollback restores settings and removes only the installed application, while preserving chats and models unless deletion is explicitly authorized. End with a structured summary of installer, hardware selection, model state, verification, approvals, privacy checks, and rollback.",
  "llama-cpp": "Codex, inspect the OS, architecture, shell, compiler toolchain, CMake, CPU instruction sets, GPU backend, existing `llama.cpp` checkout or binaries, model files, and occupied server ports. Prefer an official package when maintained for the platform, otherwise clone the canonical `ggml-org/llama.cpp` repository and build in a separate directory with CMake. Select Metal on supported Apple hardware, CUDA or Vulkan only when their toolchains are already valid, and CPU otherwise. Ask before administrator access, installing compilers or GPU toolkits, updating an existing checkout, exposing a server port, changing firewall rules, or downloading models. Preserve local patches, build directories, models, prompts, and scripts. Configure only `model_path`, `context_size`, `gpu_layers`, and `server_port` when supplied. Use `secret://provider/HUGGINGFACE_TOKEN` solely through a secure model-download mechanism; never print, echo, log, or commit it. Validate GGUF compatibility and memory requirements before loading. Verify the built `llama-cli` and `llama-server`, backend detection, model metadata, a short deterministic local completion, and localhost server health. Rollback removes only the new build, binaries, and service entries, retaining models unless deletion is approved. Finish with a structured summary of source revision, build backend, settings, checks, approvals, and rollback.",
  "jupyterlab": "Codex, begin by detecting the OS, architecture, shell, Python ownership, existing JupyterLab installations, kernels, configuration directories, workspace data, and occupied ports. Prefer a dedicated `uv` or Python virtual environment and install the official `jupyterlab` package from PyPI; use conda only when an existing conda-managed data-science environment should be preserved. Ask before administrator access, modifying system Python, replacing kernels, opening port 8888, changing firewall rules, launching a browser, or overwriting configuration. Preserve notebooks, workspaces, extensions, kernelspecs, and `jupyter_server_config` files. Configure only `workspace_dir`, `server_port`, and `default_kernel` when supplied. Reference `secret://provider/JUPYTER_TOKEN` only through secure runtime injection or protected server configuration; never print, echo, log, or store it in shell history. Bind to localhost by default and do not disable authentication. Verify with `jupyter lab --version`, `jupyter kernelspec list`, configuration inspection, a no-browser server startup, HTTP readiness, and execution of a temporary notebook through the selected kernel. Do not install extensions automatically. Rollback stops only the created server, restores config backups, removes the dedicated environment, and preserves notebooks and workspace data. Finish with a structured summary of environment, kernel, bind address, authentication, checks, approvals, and rollback.",
  "mlflow": "Codex, inspect the OS, architecture, shell, Python environment, existing MLflow installation, tracking configuration, artifact stores, backend database, running servers, and port usage before changes. Prefer installing the official `mlflow` PyPI package inside a dedicated `uv` or virtual environment; use a container only when the user already operates containerized services. Ask before administrator access, database migrations, starting a persistent service, binding port 5000, firewall changes, browser launch, or altering experiment data. Preserve the tracking database, artifact root, model registry, environment files, and existing server configuration. Configure only `tracking_uri`, `artifact_root`, and `server_port` when supplied. Use `secret://provider/AWS_ACCESS_KEY_ID` and `secret://provider/AWS_SECRET_ACCESS_KEY` only through secure credential injection for an approved S3 artifact store; never print, echo, log, or write values into commands or tracked files. Default to a local SQLite backend and filesystem artifacts unless another existing backend is explicitly selected. Verify `mlflow --version`, database connectivity, artifact write/read using a disposable experiment, server health, and read-only UI availability. Rollback stops and removes only created services and environments, restores configuration, and retains tracking data unless deletion is approved. End with a structured summary of backend, artifacts, port, credentials source, verification, approvals, and rollback.",
  "dvc": "Codex, first inspect the OS, architecture, shell, Git installation, Python tooling, existing DVC binary, repository `.dvc` configuration, cache, remotes, and tracked data state. Prefer the official `dvc` package in a dedicated `uv` or virtual environment, installing extras only for the selected remote; use Homebrew or a trusted native package when it already manages DVC. Ask before administrator access, initializing DVC, modifying `.gitignore`, changing remotes, moving cache data, downloading files, or overwriting repository configuration. Preserve `.dvc/config`, `.dvc/config.local`, lockfiles, cache links, and all data files. Configure only `cache_dir`, `default_remote`, and `analytics_enabled` when supplied. For cloud access, reference `secret://provider/AWS_ACCESS_KEY_ID`, `secret://provider/AWS_SECRET_ACCESS_KEY`, or `secret://provider/GDRIVE_CREDENTIALS_DATA` only through secure local configuration or environment injection; never print, log, or commit secret values. Verify with `dvc version`, `dvc doctor`, `dvc remote list`, cache configuration, and `dvc status` without pulling or pushing. In a disposable repository, test initialization only if approved. Rollback restores backed-up DVC and Git files, removes only the installed environment, and leaves caches and data untouched unless deletion is authorized. Finish with a structured summary of install source, repository state, cache, remote, verification, approvals, and rollback.",
  "label-studio": "Codex, inspect the OS, architecture, shell, Python and container tooling, existing Label Studio deployment, data directory, database, projects, media storage, and occupied ports. Prefer the official Docker image with persistent storage for an isolated deployment; use the official `label-studio` PyPI package in a dedicated virtual environment when containers are unsuitable. Ask before administrator access, installing or starting a container daemon, running database migrations, binding port 8080, changing firewall rules, opening a browser, or replacing data volumes. Preserve project exports, uploaded assets, user accounts, database files, and existing environment configuration. Configure only `server_port`, `data_dir`, and `database_url` when supplied. Reference `secret://provider/SECRET_KEY` and `secret://provider/DATABASE_PASSWORD` only through protected environment injection; never print, echo, log, or commit their values. Bind to localhost unless shared access is approved, and do not create public accounts automatically. Verify the package or image identity, persistent mount, database connectivity, migration status, health page, and creation of a disposable local project without uploading private data. Rollback stops and removes only resources created here, restores configuration, and retains databases and media unless deletion is authorized. End with a structured summary of deployment mode, storage, database, port, checks, approvals, and rollback.",
  "langfuse": "Codex, begin by inspecting the OS, architecture, shell, Docker or Kubernetes availability, existing Langfuse deployment, PostgreSQL and object-storage dependencies, environment files, volumes, and occupied ports. Prefer Langfuse’s official self-hosting Docker Compose configuration for a single-machine setup; use Helm only when an existing Kubernetes cluster and operations workflow are already present. Ask before administrator access, installing container tooling, starting services, database migrations, changing ports, firewall rules, browser access, or overwriting compose and environment files. Preserve the PostgreSQL database, ClickHouse or object-storage data where applicable, encryption-related settings, and existing deployment manifests. Configure only `server_port`, `telemetry_enabled`, and `public_url` when supplied. Inject `secret://provider/NEXTAUTH_SECRET`, `secret://provider/SALT`, and `secret://provider/DATABASE_URL` through a protected secrets mechanism; never print, echo, log, or commit values. Keep the service local unless an approved public URL and reverse proxy exist. Verify container health, database migration completion, HTTP readiness, login page availability, and creation of a disposable trace through a local test client without exposing credentials. Rollback removes only newly created services and manifests, restores backups, and retains databases unless deletion is approved. Finish with a structured summary of topology, persistence, URL, telemetry, checks, approvals, and rollback.",
  "flowise": "Codex, inspect the OS, architecture, shell, Node.js and container tooling, existing Flowise installation, storage directory, database, credentials store, and occupied ports before proceeding. Prefer the official Flowise Docker image with persistent storage; alternatively use the official npm package `flowise` in a dedicated user-managed Node environment. Ask before administrator access, installing Node or Docker, starting a service, binding port 3000, modifying firewall rules, opening a browser, or replacing existing data and configuration. Preserve flows, credentials, SQLite or external database files, uploads, logs, and environment files. Configure only `server_port`, `storage_path`, and `log_level` when supplied. Reference `secret://provider/FLOWISE_USERNAME`, `secret://provider/FLOWISE_PASSWORD`, and `secret://provider/OPENAI_API_KEY` only through secure environment injection or the application’s protected credential store; never print, echo, log, or commit values. Bind to localhost by default and require authentication before external exposure. Verify the installed package or image, persistent mount, database access, HTTP health, authentication behavior, and creation of a disposable flow that uses no external paid API. Rollback stops and removes only created services or environments, restores configuration, and retains flows and credentials data unless deletion is approved. End with a structured summary of runtime, storage, authentication, port, verification, approvals, and rollback.",
  "postgresql": "Codex, first inspect the OS, architecture, shell, existing PostgreSQL clients and servers, package ownership, cluster versions, data directories, running services, occupied port 5432, and backups. Prefer Homebrew `postgresql` on macOS, the distribution’s supported PostgreSQL packages on Linux, and the official PostgreSQL installer or `winget` package on Windows. Ask before administrator access, installing or enabling a service, initializing a cluster, changing ports or firewall rules, upgrading a cluster, resetting passwords, or overwriting data directories. Preserve all databases, roles, `postgresql.conf`, `pg_hba.conf`, WAL, and service definitions. Configure only `postgres_user`, `postgres_db`, `postgres_port`, and `data_directory` when supplied. Use `secret://provider/POSTGRES_PASSWORD` through a non-printing password mechanism; never echo, log, or place it in command history. Default to local connections and conservative authentication. Create the requested role or database only after confirming they do not exist. Verify with `postgres --version`, `psql --version`, `pg_isready`, service status, a local connection, and a disposable table transaction. Rollback restores configuration and service state, removes only a newly initialized empty cluster after approval, and never deletes existing data automatically. Finish with a structured summary of package, cluster, role, database, port, checks, approvals, backups, and rollback.",
  "mysql": "Codex, inspect the OS, architecture, shell, existing MySQL clients and servers, package source, data directory, configuration files, services, port 3306, and available backups before changes. Prefer Homebrew `mysql` on macOS, Oracle’s official MySQL repository or the distribution-supported package on Linux, and the official MySQL Installer or `winget` package on Windows. Ask before administrator access, adding repositories, initializing data, enabling a service, changing ports or firewall rules, running upgrades, resetting accounts, or overwriting configuration. Preserve all databases, users, grants, binary logs, `my.cnf` files, and service definitions. Configure only `mysql_user`, `mysql_database`, `mysql_port`, and `data_directory` when supplied. Use `secret://provider/MYSQL_ROOT_PASSWORD` and `secret://provider/MYSQL_PASSWORD` only through protected input or option-file mechanisms; never print, echo, log, or expose them in process arguments. Keep network access local unless explicitly approved. Create requested accounts and schemas idempotently with least-privilege grants. Verify `mysql --version`, server status, `mysqladmin ping`, local authentication, schema ownership, and a disposable transactional table. Rollback restores configs and prior service state, removes only a newly created empty instance with approval, and never deletes existing data automatically. End with a structured summary of package source, instance, accounts, schema, port, verification, approvals, and rollback.",
  "mariadb": "Codex, begin by detecting the OS, architecture, shell, existing MariaDB or MySQL binaries, package ownership, data directories, configuration, active services, port 3306 conflicts, and backups. Prefer Homebrew `mariadb` on macOS, the distribution’s MariaDB packages or MariaDB’s official repository on Linux, and MariaDB’s official MSI package on Windows. Do not install beside an active MySQL server without explaining compatibility and port risks. Ask before administrator access, adding repositories, initializing or upgrading data, enabling services, changing ports or firewall rules, resetting credentials, or replacing configuration. Preserve databases, grants, logs, `my.cnf` includes, and service definitions. Configure only `mariadb_user`, `mariadb_database`, `mariadb_port`, and `data_directory` when supplied. Handle `secret://provider/MARIADB_ROOT_PASSWORD` and `secret://provider/MARIADB_PASSWORD` through secure input or protected option files; never print, echo, log, or expose values in arguments. Bind locally by default and create roles and schemas idempotently with minimal privileges. Verify client and server versions, service health, `mariadb-admin ping`, local login, schema access, and a disposable transactional table. Rollback restores configs and service state and never removes existing data; deleting a new empty instance requires approval. Finish with a structured summary of coexistence checks, install source, instance, users, database, verification, approvals, and rollback.",
  "sqlite": "Codex, inspect the OS, architecture, shell, existing `sqlite3` binary, package ownership, linked SQLite library, target database files, and filesystem permissions before installation. On macOS prefer the system client when sufficient or Homebrew `sqlite` when a separate current CLI is requested; on Debian/Ubuntu use `apt install sqlite3`; on Fedora-family systems use `dnf install sqlite`; on Windows prefer the official SQLite tools archive or a trusted `winget` package. Ask before administrator access, changing PATH precedence, replacing binaries, opening or modifying an existing database, or overwriting files. Preserve every database and its `-wal`, `-shm`, and journal companions. Configure only `database_path`, `journal_mode`, and `synchronous_mode` when supplied. Validate requested pragmas and apply them only to the explicitly approved database after creating a consistent backup with `.backup` or the backup API. Never treat file deletion as initialization. Verify with `sqlite3 --version`, `PRAGMA integrity_check`, current journal and synchronous modes, and a disposable database that exercises a transaction, index, and rollback. Rollback restores the database backup and previous pragmas, removes only the installed CLI package, and leaves user data untouched. Finish with a structured summary of binary source, database path, backup, pragmas, integrity results, approvals, and rollback commands.",
  "redis": "Codex, first inspect the OS, architecture, shell, existing Redis binaries, package ownership, configuration files, data directory, active services, persistence files, port 6379, and backups. Prefer Homebrew `redis` on macOS, the distribution’s Redis package on Linux, and on Windows use an approved WSL or container deployment because upstream Redis does not provide a standard native Windows server. Ask before administrator access, installing a container engine, enabling or restarting a service, opening ports, changing firewall rules, replacing configuration, or deleting persistence data. Preserve `redis.conf`, ACL files, RDB snapshots, AOF files, and service definitions. Configure only `redis_port`, `maxmemory`, `persistence_mode`, and `data_directory` when supplied. Use `secret://provider/REDIS_PASSWORD` through a protected ACL or configuration mechanism; never print, echo, log, or expose it in command arguments. Bind to loopback and enable protected mode unless remote access is explicitly approved. Verify `redis-server --version`, `redis-cli --version`, config parsing, service health, authenticated `PING`, memory limits, persistence status, and a disposable key with expiry. Rollback restores configuration and prior service state, removes only newly created deployment resources, and never deletes RDB or AOF data without approval. End with a structured summary of deployment, bind address, persistence, memory, authentication, verification, approvals, and rollback.",
  "mongodb": "Codex, inspect the operating system, architecture, active shell, existing `mongod` and `mongosh` binaries, package ownership, configuration files, data directory, running services, port 27017, and available backups before making changes. On macOS prefer MongoDB Community through the official Homebrew tap; on Debian/Ubuntu or Fedora-family systems use MongoDB’s official signed repository and Community packages; on Windows use MongoDB’s official MSI or supported `winget` package. Ask before administrator access, adding repositories, enabling services, opening ports, modifying firewall rules, initializing storage, changing authentication, or overwriting configuration. Preserve databases, journals, replica-set metadata, users, `mongod.conf`, logs, and service definitions. Configure only `mongo_user`, `mongo_database`, `mongo_port`, and `data_directory` when supplied. Use `secret://provider/MONGO_INITDB_ROOT_PASSWORD` through protected initialization input; never print, echo, log, or place it in command history. Bind to localhost by default and create requested users idempotently with least privileges. Verify package identity, `mongod --version`, `mongosh --version`, service health, local authenticated connectivity, database ownership, and a disposable insert-query-delete cycle. Rollback restores config and prior service state, removes only a newly created empty instance after approval, and never deletes existing data automatically. Finish with a structured summary of package source, instance, user, database, port, checks, approvals, and rollback.",
  "clickhouse": "Codex, first inspect the OS, architecture, shell, existing `clickhouse` binaries, package source, server configuration, data and log directories, active services, ports 8123 and 9000, users, and backups. Prefer ClickHouse’s official packages and signed repository on supported Linux distributions; on macOS use Homebrew for local development; on Windows use an approved Docker or WSL deployment rather than pretending there is a standard native server installation. Ask before administrator access, adding repositories, starting services, exposing ports, changing firewall rules, creating users, or modifying storage paths. Preserve databases, parts, metadata, dictionaries, access-control files, `config.xml`, `users.xml`, and service definitions. Configure only `clickhouse_user`, `clickhouse_database`, `http_port`, and `data_directory` when provided. Inject `secret://provider/CLICKHOUSE_PASSWORD` through secure configuration or protected input and never print, echo, log, or expose it in arguments. Bind locally unless external access is approved. Verify `clickhouse client --version`, server status, HTTP health, authenticated client access, database creation, and a disposable MergeTree table with insert and aggregate queries. Rollback restores configs and service state, removes only newly created resources, and never deletes existing data without approval. End with a structured summary of deployment method, ports, storage, account, database, verification, approvals, and rollback.",
  "duckdb": "Codex, inspect the operating system, architecture, shell, existing DuckDB CLI, package ownership, target database path, extensions, and filesystem permissions before installing or opening anything. On macOS prefer Homebrew `duckdb`; on Linux use the official CLI release artifact or a trusted distribution package when available; on Windows use the official DuckDB CLI archive or supported `winget` package. Ask before administrator access, changing PATH, replacing binaries, loading unsigned extensions, opening an existing database, or overwriting files. Preserve the database file and any related temporary or WAL files. Configure only `database_path`, `extension_directory`, `memory_limit`, and `threads` when supplied. Validate that the target path is writable and create a timestamped copy before applying persistent settings or schema changes. Do not automatically install networked extensions. Verify with `duckdb --version`, `PRAGMA version`, `PRAGMA database_size`, and a disposable database that imports a tiny CSV, runs an aggregate query, and exits cleanly. For an approved existing database, run non-destructive integrity-oriented reads and confirm configured memory and thread settings. Rollback restores the database backup and previous settings, removes added extension files, and uninstalls only the CLI installed during this run. Finish with a structured summary of binary source, database path, configured resources, extensions, verification results, approvals, and rollback commands.",
  "qdrant": "Codex, begin by detecting the OS, architecture, shell, container runtime, existing Qdrant binaries or containers, storage directories, collections, snapshots, and occupied ports 6333 and 6334. Prefer Qdrant’s official Docker image with a persistent volume on macOS, Linux, or Windows; use the official Linux binary only when containers are unsuitable and the host is supported. Ask before administrator access, installing or starting Docker or Podman, creating services, exposing ports, changing firewall rules, replacing containers, or modifying existing storage. Preserve collections, payload indexes, snapshots, configuration, and volume data. Configure only `storage_path`, `http_port`, `grpc_port`, and `default_collection` when supplied. Provide `secret://provider/QDRANT_API_KEY` only through secure runtime injection; never print, echo, log, or store its value in committed compose files. Bind to localhost unless remote access is explicitly approved. Verify the image or binary identity, storage mount, readiness endpoint, API-key enforcement when enabled, collection listing, and creation plus deletion of a disposable test collection. Do not alter existing collections or snapshots. Rollback stops and removes only resources created here, restores backed-up manifests, and retains storage volumes unless deletion is separately authorized. Finish with a structured summary of deployment, persistence, ports, authentication, collection configuration, checks, approvals, and rollback.",
  "meilisearch": "Codex, inspect the OS, architecture, active shell, existing Meilisearch binary or container, database directory, dump and snapshot files, environment configuration, and port 7700 before changes. Prefer the official Meilisearch Docker image with persistent storage for cross-platform setups; on supported Linux or macOS, the official binary or Homebrew package is acceptable when a native process is preferred. Ask before administrator access, installing container tooling, starting a service, exposing ports, changing firewall rules, replacing a binary or container, or upgrading an existing database. Preserve indexes, dumps, snapshots, task history, and environment files. Configure only `database_path`, `http_address`, and `environment` when supplied. Inject `secret://provider/MEILI_MASTER_KEY` through a protected environment or secrets store; never print, echo, log, or write it into committed files. Default to production-style authentication when externally exposed and bind locally otherwise. Verify the executable or image, database mount, `/health`, version endpoint, master-key enforcement, and a disposable index with a few documents and one search query. Rollback restores manifests and configuration, removes only the installed process or container, and keeps database files unless deletion is explicitly approved. End with a structured summary of install method, bind address, environment mode, authentication, persistence, verification, approvals, and rollback.",
  "elasticsearch": "Codex, first inspect the OS, architecture, shell, existing Elasticsearch packages or containers, Java ownership, cluster state, node roles, data and log paths, security configuration, heap settings, and ports 9200 and 9300. Prefer Elastic’s official signed packages on supported Linux distributions, Homebrew for local macOS development when appropriate, and the official Docker image for isolated cross-platform deployments. Ask before administrator access, adding repositories, enabling services, joining a cluster, opening ports, changing firewall rules, generating certificates, or modifying data paths. Preserve indices, snapshots, cluster metadata, keystores, `elasticsearch.yml`, JVM options, and service definitions. Configure only `cluster_name`, `http_port`, `data_directory`, and `heap_size` when supplied. Use `secret://provider/ELASTIC_PASSWORD` through the Elasticsearch keystore or protected environment injection; never print, echo, log, or expose it in commands. Keep a new node single-node and localhost-bound unless cluster participation is explicitly approved. Verify package identity, node startup, cluster health, authenticated API access, heap allocation, and a disposable index with one document and search. Rollback restores configs and prior service state, removes only newly created deployment resources, and never deletes existing indices automatically. Finish with a structured summary of topology, security, storage, ports, heap, verification, approvals, and rollback.",
  "apache-kafka": "Codex, inspect the OS, architecture, shell, Java runtime, existing Kafka binaries, cluster metadata, broker configuration, storage directories, services, listeners, and port conflicts before installation. Prefer Apache Kafka’s official binary distribution on macOS or Linux, using a dedicated installation directory and a supported Java runtime; on Windows use an approved WSL or container workflow rather than claiming full native parity. Ask before administrator access, installing Java, formatting KRaft storage, starting brokers, changing services, opening ports, altering firewall rules, or joining an existing cluster. Preserve topic data, metadata logs, consumer offsets, `server.properties`, credentials, and service definitions. Configure only `broker_id`, `listeners`, `data_directory`, and `default_partitions` when supplied. Validate listener addresses and bind locally for a single-node development broker unless external access is approved. Do not reuse or reformat an existing cluster ID. Verify Java and Kafka scripts, configuration parsing, broker readiness, API versions, topic listing, and creation, production, consumption, and deletion of a disposable topic. Rollback stops only the created broker, restores configuration, removes new service files, and keeps log directories unless deletion is explicitly approved. Finish with a structured summary of distribution, Java, KRaft state, listeners, storage, verification, approvals, and rollback.",
  "minio": "Codex, begin by detecting the OS, architecture, shell, existing MinIO binary or container, storage volumes, service definitions, certificates, and occupied API and console ports. Prefer MinIO’s official binary on supported Linux or macOS hosts, or the official container image for macOS, Linux, and Windows environments. Ask before administrator access, installing container tooling, creating a persistent service, exposing ports, changing firewall rules, modifying storage directories, or replacing certificates and configuration. Preserve all buckets, object versions, policies, encryption metadata, certificates, and existing volumes. Configure only `data_directory`, `api_port`, `console_port`, and `server_url` when supplied. Inject `secret://provider/MINIO_ROOT_USER` and `secret://provider/MINIO_ROOT_PASSWORD` through a protected environment or secret store; never print, echo, log, or commit values. Bind to localhost unless network use is approved, and reject an empty or temporary data directory for production. Verify binary or image identity, storage access, health endpoints, console reachability, authenticated `mc` alias configuration, and creation plus deletion of a disposable bucket and object. Rollback removes only created services, containers, and aliases, restores configuration, and retains object data unless deletion is authorized. End with a structured summary of deployment, storage, endpoints, credentials handling, checks, approvals, and rollback.",
  "dbeaver": "Codex, inspect the OS, architecture, shell, existing DBeaver Community installation, Java runtime, workspace directory, drivers, secure storage, and desktop integration before making changes. On macOS prefer the official signed application or Homebrew Cask `dbeaver-community`; on Debian/Ubuntu use DBeaver’s official package or repository; on Fedora-family systems use the official RPM; on Windows prefer `winget install dbeaver.dbeaver.community`. Ask before administrator access, replacing an application, changing file associations, installing database drivers, opening a browser, or overwriting workspace settings. Preserve the workspace, connection metadata, scripts, drivers, certificates, and secure credential storage. Configure only `workspace_directory`, `default_driver`, and `editor_theme` when supplied. Do not create real database connections or store passwords unless separately approved. If the requested driver is absent, present its official Maven artifact and ask before downloading it. Verify application launch, executable version information, workspace writability, theme persistence, driver manager status, and opening a temporary SQL script without connecting to a server. Rollback restores workspace preferences, removes only drivers downloaded here, and uninstalls only the application channel added in this run. Finish with a structured summary of package source, workspace, driver state, theme, verification, approvals, and rollback steps.",
  "supabase": "Codex, first inspect the OS, architecture, shell, Docker-compatible runtime, existing Supabase CLI, local project configuration, containers, volumes, database state, and occupied ports. Prefer the official Supabase CLI through Homebrew on macOS or Linux, npm only in an existing managed Node environment, and `scoop` or the official supported Windows method; use the CLI’s documented local Docker stack rather than assembling services manually. Ask before administrator access, installing or starting Docker, initializing a project, creating containers, exposing ports, changing firewall rules, opening Studio in a browser, or overwriting `supabase/config.toml`. Preserve migrations, seed files, local volumes, functions, storage objects, and project settings. Configure only `site_url`, `api_external_url`, `studio_port`, and `database_port` when supplied. Inject `secret://provider/POSTGRES_PASSWORD`, `secret://provider/JWT_SECRET`, `secret://provider/ANON_KEY`, and `secret://provider/SERVICE_ROLE_KEY` through protected local environment handling; never print, log, or commit values. Verify `supabase --version`, config validity, container health, database readiness, API health, and Studio reachability without signing into a cloud project. Rollback stops and removes only newly created local services, restores config backups, and retains volumes unless deletion is approved. Finish with a structured summary of CLI source, project path, ports, services, verification, approvals, and rollback.",
  "docker-engine": "Codex, inspect the OS, architecture, shell, virtualization support, existing Docker, Moby, Docker Desktop, containerd, contexts, images, containers, volumes, networks, and daemon configuration before changes. On Linux prefer Docker Engine from Docker’s official signed repository or the distribution’s supported Moby packages; on macOS and Windows use Docker Desktop only when explicitly desired, because Docker Engine does not run natively without a VM layer. Ask before administrator access, adding repositories, enabling or restarting daemons, granting group access, installing Desktop, changing firewall rules, or modifying `/etc/docker/daemon.json`. Preserve all containers, images, volumes, contexts, credentials, and daemon settings. Configure only `data_root`, `default_registry`, `log_driver`, and `storage_driver` when supplied. Inject `secret://provider/DOCKER_AUTH_CONFIG` only through a protected credential mechanism; never print, echo, log, or commit it. Validate storage-driver compatibility before changing it and require explicit approval because migration may be disruptive. Verify `docker version`, `docker info`, context selection, daemon health, and a disposable official `hello-world` container. Rollback restores daemon config and prior service state, removes only newly installed packages, and never deletes images, containers, volumes, or data-root contents automatically. Finish with a structured summary of installation mode, daemon, storage, registry, verification, approvals, and rollback.",
  "podman": "Codex, begin by detecting the OS, architecture, shell, existing Podman installation, rootless configuration, containers, images, volumes, registries, and any Podman machine. On Linux prefer the distribution’s official `podman` package; on macOS and Windows use Podman Desktop or the official CLI with a managed Podman machine. Ask before administrator access, creating or resizing a VM, enabling socket services, changing subuid or subgid mappings, exposing ports, changing firewall rules, or modifying storage configuration. Preserve all containers, images, volumes, machine disks, registry credentials, and `containers.conf`, `registries.conf`, and `storage.conf`. Configure only `storage_root`, `default_registry`, `machine_resources`, and `rootless` when supplied. Provide `secret://provider/REGISTRY_AUTH_FILE` only as a protected file reference; never print, echo, log, or copy its contents. Keep rootless mode when supported and validate storage-path ownership before changes. Verify `podman version`, `podman info`, machine status where applicable, registry search settings, and a disposable `hello-world` or equivalent official container. Rollback restores configuration, removes only the machine or package created here after approval, and never deletes user images or volumes automatically. End with a structured summary of platform mode, machine, rootless state, storage, registry, checks, approvals, and rollback.",
  "kubernetes": "Codex, inspect the OS, architecture, shell, existing `kubectl`, kubeconfig files, current contexts, cluster access, container runtime, and any local or remote Kubernetes components before changing anything. Install only the official `kubectl` client unless the user explicitly approves provisioning a cluster. On macOS use Homebrew `kubectl`; on Linux use Kubernetes’ official signed package repository or release binary; on Windows prefer `winget install Kubernetes.kubectl`. Ask before administrator access, modifying kubeconfig, switching contexts, creating clusters, installing control-plane services, opening ports, changing firewall rules, or applying resources. Preserve all kubeconfig files, certificates, contexts, namespaces, and cluster state. Configure only `cluster_name`, `context_name`, `namespace`, and `container_runtime` when supplied. Use `secret://provider/KUBECONFIG` only as a protected file or environment alias; never print, log, or copy credential content. Do not create a cluster merely to verify the client. Validate with `kubectl version --client`, `kubectl config get-contexts`, and `kubectl cluster-info` only against an approved context. If no cluster exists, run client-side manifest validation on a disposable resource. Rollback restores kubeconfig backups and removes only the installed client package. Finish with a structured summary of client source, contexts, namespace, cluster reachability, verification, approvals, and rollback.",
  "k3s": "Codex, first inspect the Linux distribution, architecture, shell, existing k3s binary, service state, container runtime, cluster token files, manifests, data directories, node role, network interfaces, and ports. Install k3s only on supported Linux hosts using the official `get.k3s.io` installer or documented binary method; do not attempt native macOS or Windows installation outside an explicitly approved Linux VM. Ask before administrator access, executing the installer, creating systemd or OpenRC services, opening cluster ports, changing firewall rules, disabling swap, joining a cluster, or deleting existing state. Preserve `/etc/rancher/k3s`, `/var/lib/rancher/k3s`, manifests, certificates, kubeconfig, and service overrides. Configure only `cluster_name`, `node_role`, `server_url`, and `data_directory` when supplied. Inject `secret://provider/K3S_TOKEN` through a protected environment or token file; never print, echo, log, or expose it in process listings. Verify service health, `k3s --version`, node readiness, system pods, selected runtime, and kubeconfig access. Do not deploy workloads beyond a disposable namespace unless approved. Rollback uses the official uninstall script only after confirmation and preserves requested backups of cluster state. Finish with a structured summary of node mode, server relationship, storage, networking, checks, approvals, and rollback.",
  "kind": "Codex, inspect the OS, architecture, shell, existing `kind` and `kubectl` binaries, Docker or Podman availability, current kind clusters, kubeconfig, and available CPU, memory, and disk. Install the official kind binary through Homebrew on macOS, the signed release binary on Linux, or `winget install Kubernetes.kind` on Windows when supported. Ask before administrator access, installing or starting a container engine, creating or deleting clusters, binding host ports, changing firewall rules, or overwriting kubeconfig entries. Preserve all existing kind clusters, node containers, local images, and kubeconfig contexts. Configure only `cluster_name`, `node_count`, `kubernetes_version`, and `container_runtime` when supplied. Validate that the requested node image exists and matches kind’s documented format; do not invent a Kubernetes version or pull large images without approval. Generate a temporary cluster config rather than overwriting user files. Verify `kind version`, runtime connectivity, cluster creation, `kubectl cluster-info`, node readiness, and system pods. Use a uniquely named disposable test cluster if no persistent cluster was requested. Rollback deletes only the cluster created in this run, restores kubeconfig backups, and removes only the installed kind binary. Finish with a structured summary of runtime, image, cluster name, nodes, verification, approvals, and rollback.",
  "minikube": "Codex, begin by detecting the OS, architecture, shell, existing `minikube` and `kubectl`, virtualization or container drivers, current profiles, allocated resources, and occupied ports. Install through Homebrew on macOS, the official binary or supported package on Linux, and `winget install Kubernetes.minikube` on Windows. Ask before administrator access, installing hypervisors or container engines, creating or deleting profiles, changing firewall rules, enabling privileged drivers, or modifying kubeconfig. Preserve all existing minikube profiles, cached images, addons, certificates, and contexts. Configure only `profile_name`, `driver`, `container_runtime`, and `cluster_resources` when supplied. Select a driver only after verifying it is installed and supported; do not silently enable Hyper-V, Docker Desktop, KVM, or privileged networking. Present resource allocation before starting the cluster. Verify `minikube version`, driver status, profile creation, `minikube status`, node readiness, and system pods through `kubectl`. Enable no addons unless separately approved. Rollback stops and deletes only the profile created here, restores kubeconfig, and removes only the minikube package while leaving shared caches unless deletion is approved. End with a structured summary of driver, runtime, resources, profile, checks, approvals, and rollback.",
  "helm": "Codex, inspect the OS, architecture, shell, existing `helm` binary, plugins, repositories, cache directories, kubeconfig, current Kubernetes context, and namespace before installation. On macOS prefer Homebrew `helm`; on Linux use Helm’s official signed package repository or verified release binary; on Windows prefer `winget install Helm.Helm`. Ask before administrator access, adding repositories, installing plugins, changing kubeconfig context, logging into registries, creating namespaces, or installing, upgrading, or uninstalling releases. Preserve Helm repositories, cache, plugins, registry config, and all cluster resources. Configure only `default_namespace`, `repository_cache`, and `plugins_directory` when supplied. Reference `secret://provider/HELM_REGISTRY_CONFIG` only as a protected credential-file alias; never print, log, or copy its contents. Validate custom paths and permissions before use. Verify `helm version`, `helm env`, repository listing, plugin listing, and template rendering of a small local disposable chart without contacting a cluster. If an approved cluster is available, run `helm list` read-only in the chosen namespace. Rollback restores Helm environment and repository backups, removes only plugins or repositories added here, and uninstalls only the CLI package. Finish with a structured summary of install source, namespace, paths, repositories, cluster access, verification, approvals, and rollback.",
  "terraform": "Codex, first inspect the OS, architecture, shell, existing Terraform binaries, package source, CLI configuration, plugin cache, credentials files, workspaces, and any current infrastructure directory. Prefer HashiCorp’s official signed repository on supported Linux distributions, Homebrew `hashicorp/tap/terraform` on macOS, and `winget install Hashicorp.Terraform` on Windows. Ask before administrator access, adding repositories, replacing another Terraform binary, running `init`, changing workspaces, downloading providers, authenticating to backends, or executing `apply` or `destroy`. Preserve `.terraformrc`, `terraform.rc`, credentials, lockfiles, state files, backend configuration, and `.terraform` directories. Configure only `plugin_cache_dir`, `default_workspace`, and `parallelism` when supplied. Use `secret://provider/TF_TOKEN_app_terraform_io`, `secret://provider/AWS_ACCESS_KEY_ID`, and `secret://provider/AWS_SECRET_ACCESS_KEY` only through secure environment or credential helpers; never print, log, or commit them. Verify `terraform version`, CLI config parsing, and `terraform fmt -check` plus `terraform validate` on a disposable local configuration without remote providers. Rollback restores CLI config, removes only the installed binary, and never alters state or infrastructure. Finish with a structured summary of binary source, workspace preference, cache, credentials aliases, checks, approvals, and rollback.",
  "opentofu": "Codex, inspect the OS, architecture, shell, existing `tofu` and Terraform binaries, package ownership, CLI config, provider cache, credentials, workspaces, and current infrastructure directories before changes. Prefer OpenTofu’s official signed package repository on supported Linux systems, Homebrew `opentofu` on macOS, and the official Windows package or supported `winget` entry. Ask before administrator access, adding repositories, changing PATH precedence relative to Terraform, running initialization, switching workspaces, downloading providers, authenticating to backends, or executing plan-affecting commands such as `apply` or `destroy`. Preserve `.tofurc`, Terraform-compatible CLI configs, lockfiles, state, backend files, and provider directories. Configure only `plugin_cache_dir`, `default_workspace`, and `parallelism` when supplied. Reference `secret://provider/TF_TOKEN_app_terraform_io`, `secret://provider/AWS_ACCESS_KEY_ID`, and `secret://provider/AWS_SECRET_ACCESS_KEY` solely through protected environment or credential mechanisms; never print, echo, log, or commit values. Verify `tofu version`, configuration parsing, formatting, and validation using a disposable local-only module. Do not automatically migrate Terraform state. Rollback restores config and PATH order, removes only the installed OpenTofu package, and leaves infrastructure state untouched. Finish with a structured summary of coexistence, install source, workspace, cache, verification, approvals, and rollback.",
  "ansible": "Codex, begin by detecting the OS, architecture, shell, Python ownership, existing Ansible installation, collections, roles, inventory files, configuration, SSH setup, and any active automation repository. Prefer `pipx install ansible` or a dedicated `uv` environment for user-managed installs; use the distribution package only when that is the established system-management channel. On Windows install Ansible only inside an approved WSL environment because the control node is not natively supported. Ask before administrator access, installing Python tooling, modifying SSH configuration, creating inventory files, downloading collections, connecting to hosts, or executing playbooks. Preserve `ansible.cfg`, inventories, vault files, roles, collections, and known-hosts data. Configure only `inventory_path`, `remote_user`, `roles_path`, and `forks` when supplied. Reference `secret://provider/ANSIBLE_VAULT_PASSWORD_FILE` only as a protected file alias; never print, read aloud, log, or copy its contents. Verify `ansible --version`, configuration resolution with `ansible-config dump --only-changed`, inventory parsing, and `ansible-inventory --graph`. Run `ansible localhost -m ping --connection=local` only; do not contact remote systems without approval. Rollback restores config backups, removes only installed environments or collections, and leaves inventories and credentials untouched. Finish with a structured summary of install method, Python environment, inventory, SSH scope, verification, approvals, and rollback.",
  "pulumi": "Codex, inspect the operating system, architecture, active shell, existing `pulumi` binary, language runtimes, backend configuration, stacks, plugins, credentials files, and current infrastructure repositories before changing anything. Prefer Homebrew `pulumi` on macOS, Pulumi’s official signed installation method on Linux, and `winget install Pulumi.Pulumi` on Windows. Ask before administrator access, executing a remote installer, changing PATH, logging into Pulumi Cloud, selecting a backend, downloading providers, switching stacks, or running `up` or `destroy`. Preserve `Pulumi.yaml`, stack files, state, plugins, policy packs, and credentials. Configure only `backend_url`, `default_organization`, and `default_stack` when supplied. Use `secret://provider/PULUMI_ACCESS_TOKEN` and `secret://provider/PULUMI_CONFIG_PASSPHRASE` only through protected environment or credential mechanisms; never print, echo, log, or commit their values. Prefer a local backend for verification unless an approved backend already exists. Verify `pulumi version`, plugin discovery, backend status, and stack listing. Create a disposable local project that previews a file resource without applying changes. Rollback restores credentials and config backups, removes only plugins or the CLI installed here, and never deletes stack state automatically. Finish with a structured summary of installation source, backend, organization, stack, verification, approvals, and rollback.",
  "argo-cd": "Codex, first inspect the OS, architecture, shell, existing `argocd` CLI, Kubernetes contexts, namespaces, Argo CD installations, Helm releases, manifests, repositories, and occupied service ports. Install the official Argo CD CLI through Homebrew on macOS, the verified release binary on Linux, or a supported `winget` package on Windows. Do not install the in-cluster controller unless an approved Kubernetes context exists. Ask before administrator access, creating namespaces, applying manifests, installing Helm charts, opening ports, changing firewall rules, port-forwarding, browser sign-in, or modifying cluster resources. Preserve existing Argo CD applications, projects, repositories, secrets, RBAC, and certificates. Configure only `server_address`, `default_project`, and `default_namespace` when supplied. Use `secret://provider/ARGOCD_AUTH_TOKEN` through protected environment injection; never print, echo, log, or store it in shell history. Verify `argocd version --client`, kube-context access, namespace existence, and read-only application listing when authenticated. If no server is available, validate official installation manifests with client-side Kubernetes dry-run only. Rollback restores kubeconfig and CLI config backups, removes only the installed CLI or newly created namespace after approval, and never deletes existing applications. Finish with a structured summary of CLI source, cluster context, namespace, server, authentication, checks, approvals, and rollback.",
  "prometheus": "Codex, inspect the OS, architecture, shell, existing Prometheus binaries or containers, configuration files, rule files, TSDB storage, service definitions, scrape targets, and port 9090 before making changes. Prefer Prometheus’s official release archive on supported Linux or macOS systems, or the official container image for isolated cross-platform deployment. On Windows use the official Windows archive when supported. Ask before administrator access, creating users or services, binding ports, changing firewall rules, replacing configuration, reloading rules, or moving storage. Preserve `prometheus.yml`, alerting and recording rules, TSDB blocks, snapshots, and service files. Configure only `scrape_interval`, `retention_time`, `storage_path`, and `web_port` when supplied. Validate durations and storage capacity before applying them. Bind to localhost unless external monitoring access is explicitly approved. Verify `prometheus --version`, configuration syntax with `promtool check config`, rule syntax with `promtool check rules`, service readiness, `/api/v1/status/config`, and ingestion from a disposable local scrape target. Do not alter existing targets without approval. Rollback restores config and prior service state, removes only binaries or containers created here, and preserves TSDB data unless deletion is separately authorized. End with a structured summary of deployment method, storage, retention, scrape settings, port, verification, approvals, and rollback.",
  "grafana": "Codex, begin by inspecting the OS, architecture, shell, existing Grafana installation, package ownership, plugins, data directory, provisioning files, database, service state, and port 3000. Prefer Grafana’s official signed repository on supported Linux distributions, Homebrew `grafana` for local macOS use, the official Windows installer, or the official container image when isolation is preferred. Ask before administrator access, adding repositories, enabling services, exposing ports, changing firewall rules, opening a browser, running database migrations, installing plugins, or overwriting provisioning files. Preserve dashboards, users, alert rules, plugins, SQLite or external database data, and `grafana.ini`. Configure only `admin_user`, `server_port`, `data_path`, and `public_url` when supplied. Use `secret://provider/GF_SECURITY_ADMIN_PASSWORD` and `secret://provider/GF_DATABASE_PASSWORD` only through protected environment or secrets injection; never print, echo, log, or commit values. Bind locally unless external access is approved. Verify package identity, service health, `/api/health`, database connectivity, configured URL, and login-page availability without browser sign-in. Rollback restores config and service state, removes only installed packages or containers, and retains dashboards and data unless deletion is authorized. Finish with a structured summary of installation, database, data path, URL, port, verification, approvals, and rollback.",
  "caddy": "Codex, inspect the OS, architecture, shell, existing `caddy` binary, package source, Caddyfile, JSON configuration, certificates, service state, listeners, and ports 80 and 443 before installation. Prefer Caddy’s official signed repository on supported Linux distributions, Homebrew `caddy` on macOS, and the official Windows package or `winget` entry. Ask before administrator access, binding privileged ports, creating or restarting services, changing firewall rules, obtaining public certificates, modifying DNS, replacing reverse-proxy routes, or overwriting configuration. Preserve the Caddyfile, autosave JSON, certificate storage, logs, and service definitions. Configure only `domain_name`, `http_port`, `https_port`, and `config_path` when supplied. Use `secret://provider/CLOUDFLARE_API_TOKEN` solely through a protected environment or service credential mechanism for an approved DNS challenge; never print, echo, log, or commit it. Default to localhost and a non-privileged test port until public exposure is approved. Verify `caddy version`, `caddy validate`, `caddy adapt`, a temporary local file-server response, and service health where enabled. Rollback restores configuration and prior service state, removes only created service files or packages, and preserves certificates. Finish with a structured summary of install source, domains, ports, TLS approach, config path, verification, approvals, and rollback.",
  "nextcloud": "Codex, first inspect the OS, architecture, shell, existing Nextcloud deployment, web server, PHP runtime, database, containers, data directory, configuration, apps, and occupied ports. Prefer Nextcloud’s official Docker image with persistent volumes for a new cross-platform deployment; use the official archive only when an existing Linux web stack is already managed. Ask before administrator access, installing container or web-server services, creating databases, binding ports, changing firewall rules, opening a browser, running upgrades, or overwriting `config.php`. Preserve the data directory, database, installed apps, themes, certificates, and existing volumes. Configure only `admin_user`, `trusted_domains`, `data_directory`, and `server_port` when supplied. Inject `secret://provider/NEXTCLOUD_ADMIN_PASSWORD` and `secret://provider/MYSQL_PASSWORD` through protected runtime secrets; never print, echo, log, or commit values. Keep the service localhost-only until trusted domains and reverse proxy settings are approved. Verify container or web-stack health, `occ status`, database connectivity, trusted-domain configuration, writable storage, and the login endpoint without browser sign-in. Rollback stops and removes only newly created services and restores configuration, while retaining database and files unless deletion is explicitly authorized. Finish with a structured summary of deployment mode, database, storage, domains, port, verification, approvals, and rollback.",
  "appflowy": "Codex, inspect the operating system, architecture, shell, existing AppFlowy desktop installation, workspace data, local database, plugins, synchronization configuration, and application settings before changes. Use AppFlowy’s official signed desktop release for supported macOS, Windows, or Linux platforms; prefer the native package format and verify publisher or checksum information when available. Ask before administrator access, replacing the application, changing file associations, enabling startup behavior, opening a browser, signing into cloud synchronization, or moving workspace data. Preserve all local workspaces, documents, databases, templates, and account settings. Configure only `workspace_directory`, `default_language`, and `appearance` when supplied. Validate the workspace destination, ensure adequate disk space, and copy rather than move existing data unless migration is explicitly approved. Do not enable cloud sync or account login automatically. Verify application launch, workspace discovery, database readability, language and appearance persistence, and creation plus deletion of a temporary local page. Confirm the test remains local. Rollback restores backed-up settings, reverts any workspace path change, and removes only the application installed in this run, leaving user workspaces untouched. Finish with a structured summary of package source, workspace, language, appearance, migration actions, verification, approvals, and rollback.",
  "logseq": "Codex, begin by detecting the OS, architecture, shell, existing Logseq application, graph directories, configuration, plugins, themes, journals, and synchronization settings. Install the official signed Logseq desktop release using the native package for supported macOS, Linux, or Windows systems. Ask before administrator access, replacing an existing app, registering file associations, opening a browser, enabling account synchronization, installing plugins, or modifying graph files. Preserve every graph, `config.edn`, custom CSS, plugin data, assets, and Git history. Configure only `graph_directory`, `preferred_format`, and `default_theme` when supplied. Validate that the selected graph directory is not nested inside another graph and that `preferred_format` is supported. Never convert Markdown and Org files in bulk without explicit approval and a full backup. Apply theme changes through existing settings without overwriting custom CSS. Verify application launch, graph indexing, journal creation in a disposable test graph, linked-page behavior, and persistence of format and theme settings. Do not touch the user’s primary graph for functional testing. Rollback restores settings and removes only the temporary graph and installed application, while leaving existing notes and plugins intact. Finish with a structured summary of installation source, graph paths, format, theme, verification, approvals, backups, and rollback.",
  "joplin": "Codex, inspect the OS, architecture, shell, existing Joplin desktop or terminal installation, profile directory, notes database, resources, plugins, encryption state, synchronization target, and external editor settings before changing anything. Prefer the official signed Joplin desktop package on macOS, Windows, and supported Linux systems; use the official terminal application only when a CLI workflow is requested. Ask before administrator access, replacing an app, changing sync targets, opening a browser, authenticating to a cloud provider, enabling encryption, installing plugins, or modifying note data. Preserve the profile, database, resources, encryption keys, templates, and synchronization metadata. Configure only `notes_directory`, `sync_target`, `default_editor`, and `theme` when supplied. Use `secret://provider/JOPLIN_TOKEN` only through a protected environment or application credential store; never print, echo, log, or commit it. Do not run synchronization until the target and conflict strategy are approved. Verify application launch or `joplin version`, profile readability, editor path, theme persistence, and creation plus export of a disposable note. Rollback restores profile settings, removes only the temporary note and installed application, and never deletes synchronized or encrypted data. Finish with a structured summary of package type, profile, sync state, editor, theme, verification, approvals, and rollback.",
  "memos": "Codex, first inspect the OS, architecture, shell, existing Memos deployment, Docker or Podman availability, database and data directory, environment files, service definitions, and occupied ports. Prefer the official Memos container image with a persistent data mount; use the official binary only on a supported host when containers are unsuitable. Ask before administrator access, installing or starting a container engine, creating services, exposing ports, changing firewall rules, opening a browser, upgrading the database, or replacing volumes. Preserve the database, attachments, user accounts, configuration, and backups. Configure only `server_port`, `data_directory`, and `public_url` when supplied. Bind to localhost unless an approved reverse proxy or LAN use requires external access. Validate that the public URL matches the selected port and protocol. Verify the image or binary source, persistent mount, application startup, HTTP readiness, database migration status, and creation plus deletion of a disposable memo through the local interface or API. Do not create public users or enable external authentication automatically. Rollback stops and removes only the container, binary, or service created here, restores manifests, and keeps all data files unless deletion is explicitly authorized. Finish with a structured summary of deployment method, storage, URL, port, migration state, verification, approvals, and rollback.",
  "vikunja": "Codex, inspect the OS, architecture, shell, existing Vikunja installation, container runtime, database, files storage, configuration, users, mail settings, and occupied ports before making changes. Prefer Vikunja’s official Docker Compose deployment with persistent volumes; use the official Linux binary only when a native service is specifically preferred. Ask before administrator access, installing container tooling, creating or restarting services, running database migrations, exposing ports, changing firewall rules, opening a browser, or replacing configuration. Preserve tasks, projects, attachments, users, database records, and configuration files. Configure only `server_port`, `database_type`, `public_url`, and `files_path` when supplied. Inject `secret://provider/VIKUNJA_SERVICE_JWTSECRET` and `secret://provider/VIKUNJA_DATABASE_PASSWORD` through protected secrets or environment handling; never print, echo, log, or commit values. Keep registration and external access unchanged unless separately approved. Verify image or binary identity, database connectivity, migration completion, file-storage permissions, HTTP health, and creation plus deletion of a disposable local task and project. Rollback restores configuration, stops and removes only newly created services, and retains databases and files unless deletion is authorized. Finish with a structured summary of deployment, database, files path, URL, port, checks, approvals, and rollback.",
  "plane": "Codex, begin by detecting the OS, architecture, shell, Docker and Compose availability, existing Plane deployment, PostgreSQL data, object storage, environment files, domains, and occupied ports. Prefer Plane’s official self-hosting Docker Compose setup and documented installer; do not assemble unofficial service combinations. Ask before administrator access, installing or starting Docker, creating databases or buckets, running migrations, binding ports, changing firewall rules, opening a browser, or overwriting compose and environment files. Preserve projects, issues, users, uploads, database volumes, Redis data, and existing proxy settings. Configure only `domain_name`, `server_port`, `database_name`, and `file_storage` when supplied. Inject `secret://provider/SECRET_KEY`, `secret://provider/POSTGRES_PASSWORD`, and `secret://provider/AWS_SECRET_ACCESS_KEY` only through protected secret files or runtime injection; never print, echo, log, or commit values. Keep the deployment local unless the domain, TLS, and reverse proxy are approved. Verify container health, migration completion, database connectivity, storage write access, API readiness, and the login page without creating cloud accounts. Rollback removes only newly created containers and manifests, restores backups, and retains volumes and uploads unless deletion is explicitly authorized. Finish with a structured summary of topology, domain, database, storage, ports, verification, approvals, and rollback.",
  "taiga": "Codex, inspect the OS, architecture, shell, Docker and Compose versions, existing Taiga deployment, PostgreSQL and RabbitMQ data, media volumes, configuration, domains, and occupied ports before proceeding. Prefer the official `taiga-docker` repository and its documented Compose workflow. Ask before administrator access, cloning over an existing directory, installing or starting container services, creating databases, running migrations, exposing ports, changing firewall rules, opening a browser, or overwriting environment files. Preserve projects, users, attachments, database volumes, event queues, static files, and reverse-proxy configuration. Configure only `domain_name`, `server_port`, and `default_language` when supplied. Use `secret://provider/SECRET_KEY`, `secret://provider/POSTGRES_PASSWORD`, and `secret://provider/RABBITMQ_PASS` only through protected environment files with restrictive permissions; never print, echo, log, or commit values. Bind locally unless the domain and TLS path are approved. Verify repository origin, container health, database migrations, asynchronous-events service, media permissions, API readiness, and the web login page without creating external accounts. Rollback restores compose and environment backups, removes only containers created here, and preserves volumes and media unless deletion is authorized. Finish with a structured summary of deployment source, domain, language, services, storage, verification, approvals, and rollback.",
  "cal-com": "Codex, first inspect the OS, architecture, shell, Node and container tooling, existing Cal.com deployment, database, environment files, calendar integrations, public URL, and occupied ports. Prefer Cal.com’s official Docker self-hosting workflow for an isolated deployment; use the official repository with its documented package manager only when a development installation is explicitly requested. Ask before administrator access, installing Docker or Node tooling, creating databases, running migrations, exposing ports, changing firewall rules, opening a browser, enabling OAuth providers, or overwriting environment files. Preserve bookings, users, teams, database data, integrations, and uploaded assets. Configure only `public_url`, `server_port`, and `default_timezone` when supplied. Inject `secret://provider/NEXTAUTH_SECRET`, `secret://provider/CALENDSO_ENCRYPTION_KEY`, and `secret://provider/DATABASE_URL` through protected runtime secrets; never print, echo, log, or commit them. Do not configure calendar-provider credentials or browser sign-in without separate approval. Verify container or application health, migration completion, URL and timezone settings, database connectivity, and the public scheduling page using a disposable local account only if approved. Rollback stops and removes only created services, restores configuration, and retains databases unless deletion is authorized. Finish with a structured summary of runtime, URL, timezone, database, integrations state, verification, approvals, and rollback.",
  "paperless-ngx": "Codex, inspect the OS, architecture, shell, existing Paperless-ngx deployment, Docker availability, PostgreSQL or SQLite database, Redis, consume and media directories, OCR languages, and occupied ports. Prefer the official Paperless-ngx Docker Compose installation with persistent volumes. Ask before administrator access, installing or starting Docker, creating services, running migrations, exposing ports, changing firewall rules, opening a browser, importing documents, or overwriting compose and environment files. Preserve documents, originals, thumbnails, metadata, correspondents, tags, database volumes, and export backups. Configure only `consume_directory`, `media_root`, `server_port`, and `ocr_language` when supplied. Inject `secret://provider/PAPERLESS_SECRET_KEY`, `secret://provider/PAPERLESS_DBPASS`, and `secret://provider/PAPERLESS_ADMIN_PASSWORD` through protected environment or secret files; never print, echo, log, or commit values. Confirm OCR language packages before installation and ask before downloading additional language data. Verify container health, migration completion, consume-directory permissions, OCR capability, database connectivity, and the web login page. Process only a disposable sample document, never user files, during verification. Rollback restores manifests, removes only created containers, and retains documents and databases unless deletion is explicitly authorized. Finish with a structured summary of deployment, storage paths, OCR, port, verification, approvals, and rollback.",
  "penpot": "Codex, begin by detecting the OS, architecture, shell, Docker and Compose availability, existing Penpot deployment, PostgreSQL data, assets storage, configuration, public URI, and occupied ports. Prefer Penpot’s official Docker Compose self-hosting configuration. Ask before administrator access, installing or starting container services, creating databases, running migrations, binding ports, changing firewall rules, opening a browser, enabling email registration, or overwriting environment files. Preserve design files, teams, user accounts, assets, database volumes, and existing reverse-proxy configuration. Configure only `public_uri`, `server_port`, and `database_name` when supplied. Inject `secret://provider/PENPOT_SECRET_KEY` and `secret://provider/POSTGRES_PASSWORD` through protected runtime secrets or permission-restricted environment files; never print, echo, log, or commit values. Keep the deployment localhost-only until the public URI, TLS, and proxy settings are approved. Verify official image sources, container health, database migration completion, asset-storage permissions, frontend and backend readiness, and the login page without creating external accounts. Rollback restores compose and environment backups, removes only services created here, and preserves all database and asset volumes unless deletion is explicitly authorized. Finish with a structured summary of deployment source, URI, database, storage, port, verification, approvals, and rollback.",
  "blender": "Codex, inspect the operating system, architecture, shell, GPU hardware, drivers, existing Blender installation, preferences, add-ons, asset libraries, workspaces, and render devices before changes. Use Blender’s official signed installer or archive for supported macOS, Linux, or Windows platforms; Homebrew Cask is acceptable on macOS when it already manages desktop applications. Ask before administrator access, replacing an application, changing file associations, installing add-ons, downloading assets, enabling scripts, or modifying GPU drivers. Preserve startup files, user preferences, add-ons, templates, recent files, and asset catalogs. Configure only `render_engine`, `asset_library_path`, `default_workspace`, and `gpu_backend` when supplied. Validate that the requested engine and backend are supported by detected hardware; do not silently enable experimental features. Add asset libraries through Blender preferences without moving existing files. Verify application launch, `blender --version`, background-mode execution, preference loading, GPU-device detection, and rendering of a temporary built-in scene to a disposable output path. Do not open or modify user `.blend` files. Rollback restores backed-up preferences, removes only add-ons or asset entries created here, and uninstalls only the application channel added in this run. Finish with a structured summary of package source, GPU, engine, workspace, assets, verification, approvals, and rollback.",
  "gimp": "Codex, first inspect the OS, architecture, shell, existing GIMP installation, user profile, plugins, scripts, brushes, themes, color profiles, and file associations. Install the official GIMP package for the detected supported platform: the signed application on macOS, distribution packages or official Flatpak on Linux, and the official Windows installer or supported `winget` package. Ask before administrator access, replacing an application, changing file associations, installing Flatpak, enabling third-party plugins, or overwriting profile files. Preserve all user resources, shortcuts, templates, recent files, and plugin configuration. Configure only `theme`, `plugin_directory`, and `default_export_format` when supplied. Validate the export format and plugin directory, and add paths without removing existing search locations. Do not install external plugins automatically. Verify application launch, version information, profile loading, theme persistence, plugin-directory discovery, and a noninteractive export of a generated temporary image to the selected format. Never open or alter user artwork for testing. Rollback restores profile backups, removes only paths or files added here, and uninstalls only the package installed during this run. Finish with a structured summary of install source, profile path, theme, plugin directories, export format, verification, approvals, and rollback.",
  "inkscape": "Codex, inspect the OS, architecture, shell, existing Inkscape installation, user preferences, extensions, templates, themes, color profiles, and file associations before making changes. Prefer the official signed application on macOS, the distribution package or official Flatpak on Linux, and the official Windows installer or supported `winget` package. Ask before administrator access, installing Flatpak, replacing an application, changing SVG associations, installing extensions, or overwriting preferences and templates. Preserve custom palettes, extensions, symbols, templates, keyboard mappings, and recent-document data. Configure only `default_template`, `theme`, and `extension_directory` when supplied. Confirm that the requested template is a valid SVG and add extension paths without removing existing locations. Do not execute untrusted extension scripts. Verify Inkscape launch, `inkscape --version`, preference parsing, theme persistence, template loading, extension discovery, and command-line export of a disposable SVG to PNG or PDF. Do not open or rewrite user artwork during verification. Rollback restores preference backups, removes only extension paths or templates added here, and uninstalls only the application installed in this run. Finish with a structured summary of package source, profile, template, theme, extension paths, verification, approvals, and rollback.",
  "krita": "Codex, begin by detecting the OS, architecture, shell, GPU environment, existing Krita installation, resource folders, workspaces, color profiles, brushes, plugins, and file associations. Use Krita’s official signed application or package for supported macOS, Linux, and Windows systems; prefer the distribution package or official Flatpak on Linux and a supported `winget` package on Windows. Ask before administrator access, replacing an application, changing file associations, installing Flatpak, importing resource bundles, or overwriting settings. Preserve brushes, palettes, templates, workspaces, shortcuts, autosaves, and custom color profiles. Configure only `workspace`, `resource_directory`, `color_profile`, and `canvas_only_mode` when supplied. Validate the requested color profile and resource directory before registering them, and do not move existing resources automatically. Verify application launch, `krita --version` where available, workspace selection, resource discovery, color-profile loading, and creation plus export of a disposable blank document through command-line or GUI-safe automation. Never modify user artwork for testing. Rollback restores backed-up settings, removes only resources or paths added here, and uninstalls only the package installed during this run. Finish with a structured summary of installation source, workspace, resources, color management, canvas mode, verification, approvals, and rollback.",
  "obs-studio": "Codex, inspect the operating system, architecture, shell, GPU, audio devices, cameras, existing OBS Studio installation, profiles, scene collections, plugins, recording paths, and hardware encoders before changing anything. Use the official signed OBS package on macOS or Windows; on Linux prefer the distribution package or official Flatpak when appropriate. Ask before administrator access, replacing the application, changing file associations, installing plugins, accessing cameras or microphones, opening firewall ports, enabling virtual cameras, browser sign-in, or overwriting profiles. Preserve all scenes, sources, filters, hotkeys, stream settings, and recordings. Configure only `recording_path`, `video_resolution`, `video_fps`, and `hardware_encoder` when supplied. Validate encoder support against detected hardware and never silently reduce quality. Reference `secret://provider/TWITCH_STREAM_KEY` and `secret://provider/YOUTUBE_STREAM_KEY` only through OBS’s protected service configuration after approval; never print, echo, log, or expose them. Verify application launch, profile loading, device enumeration, encoder availability, and a short local recording using a temporary scene without streaming. Rollback restores backed-up profiles, removes only plugins added here, and uninstalls only this run’s package. Finish with a structured summary of installer, devices, encoder, recording settings, verification, approvals, and rollback.",
  "kdenlive": "Codex, first inspect the OS, architecture, shell, GPU capabilities, existing Kdenlive installation, project folders, proxy settings, render profiles, plugins, and media cache. On Linux prefer the distribution package or official AppImage/Flatpak; on macOS and Windows use Kdenlive’s official signed packages. Ask before administrator access, replacing the application, installing codecs, changing file associations, enabling hardware acceleration, downloading effects, or overwriting project settings. Preserve all `.kdenlive` projects, custom profiles, keyboard shortcuts, title templates, proxies, and render presets. Configure only `project_directory`, `proxy_enabled`, `default_profile`, and `render_directory` when supplied. Validate the selected project profile, frame rate, storage capacity, and proxy path before applying changes. Do not modify existing project files or regenerate proxies without approval. Verify application launch, version information, MLT backend detection, codec availability, project and render directory permissions, and export of a short disposable color clip to a temporary file. Confirm that hardware acceleration is actually supported before enabling it. Rollback restores backed-up preferences, removes only codecs or packages installed here, and leaves projects and media untouched. Finish with a structured summary of package source, profile, proxy state, render path, codec checks, approvals, and rollback.",
  "audacity": "Codex, inspect the operating system, architecture, shell, existing Audacity installation, audio input and output devices, sample-rate support, plugins, preferences, and project directories before changes. Use Audacity’s official signed installer on macOS or Windows; on Linux prefer the distribution package or official AppImage when suitable. Ask before administrator access, replacing the application, changing file associations, granting microphone access, installing FFmpeg or plugins, enabling telemetry-related options, or overwriting preferences. Preserve projects, autosaves, macros, effects, plugin paths, and custom shortcuts. Configure only `recording_device`, `playback_device`, `sample_rate`, and `project_directory` when supplied. Confirm that requested devices are present and that the sample rate is supported by both input and output hardware. Never open or rewrite an existing project for testing. Verify application launch, device enumeration, plugin scanning, recording and playback through a short disposable mono test, and export to an uncompressed temporary file. Ask before accessing a live microphone in restricted environments. Rollback restores preference backups, removes only plugins or codec components added here, and uninstalls only the installed package while leaving projects intact. Finish with a structured summary of installer, devices, sample rate, project path, verification, approvals, and rollback.",
  "ffmpeg": "Codex, begin by detecting the OS, architecture, shell, existing `ffmpeg`, `ffprobe`, and `ffplay` binaries, package ownership, enabled codecs, hardware acceleration APIs, and PATH precedence. On macOS prefer Homebrew `ffmpeg`; on Debian or Ubuntu use `apt install ffmpeg`; on Fedora-family systems use the appropriate trusted repository package; on Windows prefer `winget install Gyan.FFmpeg` or an official trusted build source. Ask before administrator access, adding repositories, replacing another build, modifying PATH, or installing patent-encumbered codec packages. Preserve existing scripts, aliases, presets, and binaries. Configure only `hardware_acceleration`, `default_video_codec`, `default_audio_codec`, and `output_directory` when supplied. Validate requested codecs with `ffmpeg -encoders` and hardware support with `ffmpeg -hwaccels`; do not claim unavailable acceleration. Avoid creating global aliases that hide explicit codec choices unless approved. Verify with `ffmpeg -version`, `ffprobe -version`, codec listings, and generation plus transcoding of a short synthetic test pattern and sine wave into the selected output directory. Rollback restores PATH and shell backups and removes only the package installed here. Finish with a structured summary of build source, enabled codecs, acceleration, output path, test results, approvals, and rollback.",
  "trivy": "Codex, inspect the OS, architecture, shell, existing `trivy` binary, package source, cache, configuration, ignore files, container runtime, and CI-related settings before installation. On macOS prefer Homebrew `trivy`; on Linux use Aqua Security’s official signed repository or verified release package; on Windows prefer `winget install AquaSecurity.Trivy`. Ask before administrator access, adding repositories, downloading vulnerability databases, scanning private directories, accessing container sockets, modifying ignore files, or uploading results. Preserve existing `trivy.yaml`, `.trivyignore`, caches, policies, and CI configuration. Configure only `cache_directory`, `severity_threshold`, `ignore_unfixed`, and `output_format` when supplied. Reference `secret://provider/GITHUB_TOKEN` only through secure environment injection for approved GitHub rate-limit or repository access; never print, echo, log, or commit it. Verify `trivy --version`, configuration parsing, database metadata, and a scan of a disposable local filesystem or a small approved public image. Do not scan the user’s home directory or private repositories automatically. Rollback restores config backups, removes only downloaded test artifacts and the installed package, and retains caches unless deletion is approved. Finish with a structured summary of install source, database status, scan target, policies, findings count, approvals, and rollback.",
  "bitwarden-server": "Codex, first inspect the OS, architecture, shell, Docker availability, existing Bitwarden self-hosted deployment, database provider, volumes, certificates, domain configuration, and occupied ports. Prefer Bitwarden’s official self-host installation script and documented Docker-based deployment on a supported Linux host; do not invent a lightweight native desktop-server install. Ask before administrator access, installing or starting Docker, creating services, binding ports, changing firewall rules, requesting certificates, opening a browser, running migrations, or overwriting deployment files. Preserve attachments, database backups, configuration, certificates, logs, and container volumes. Configure only `domain_name`, `database_provider`, and `server_port` when supplied. Inject `secret://provider/GLOBALSETTINGS_IDENTITYSERVER_CERTIFICATEPASSWORD` and `secret://provider/GLOBALSETTINGS_SQLSERVER_SA_PASSWORD` through protected secret or environment files with restrictive permissions; never print, echo, log, or commit values. Keep the service local until domain, TLS, and external exposure are approved. Verify official image sources, container health, database connectivity, migration status, HTTPS or local endpoint readiness, and the web vault login page without creating accounts. Rollback restores deployment files, removes only newly created services, and retains database and attachments unless deletion is explicitly authorized. Finish with a structured summary of topology, domain, database, ports, verification, approvals, and rollback.",
  "vaultwarden": "Codex, inspect the OS, architecture, shell, Docker or Podman availability, existing Vaultwarden deployment, data volume, database, attachments, environment files, reverse proxy, and occupied ports. Prefer the official `vaultwarden/server` container image with a persistent `/data` mount; use a native binary only on a supported host when containers are unsuitable. Ask before administrator access, installing container tooling, starting services, exposing ports, changing firewall rules, opening a browser, enabling registrations, or overwriting environment files. Preserve the database, attachments, RSA keys, sends, icon cache, and backups. Configure only `domain_name`, `server_port`, `data_directory`, and `signups_allowed` when supplied. Inject `secret://provider/ADMIN_TOKEN`, `secret://provider/DATABASE_URL`, and `secret://provider/SMTP_PASSWORD` through protected runtime secrets; never print, echo, log, or commit values. Default registrations to their existing state and keep the service localhost-bound until TLS and proxy settings are approved. Verify image identity, persistent volume access, database health, `/alive` or equivalent readiness, admin endpoint protection, and web vault loading without creating a real vault. Rollback removes only created containers and manifests, restores backups, and retains all data unless deletion is authorized. Finish with a structured summary of runtime, storage, domain, registration state, authentication, verification, approvals, and rollback.",
  "keepassxc": "Codex, begin by detecting the OS, architecture, shell, existing KeePassXC installation, database files, key files, browser integration, auto-lock settings, and file associations. Use the official signed application on macOS or Windows; on Linux prefer the distribution package, official AppImage, or Flatpak. Ask before administrator access, replacing the application, changing `.kdbx` associations, enabling browser integration, installing browser extensions, opening a browser, or modifying an existing database. Preserve all databases, key files, backups, custom icons, settings, and browser pairing records. Configure only `database_path`, `browser_integration`, `auto_lock_minutes`, and `key_file_path` when supplied. Never request, display, log, or store a master password. Validate paths and permissions, and require approval before creating a new key file or enabling browser communication. Verify application launch, version information, settings persistence, auto-lock configuration, and opening a disposable temporary database created solely for testing. Do not unlock or alter the user’s real database. Rollback restores preference backups, disables only integrations enabled here, removes any disposable test database, and uninstalls only this run’s package. Finish with a structured summary of installer, database and key-file paths, browser integration state, lock policy, verification, approvals, and rollback.",
  "wireguard": "Codex, inspect the OS, architecture, shell, existing WireGuard tools, kernel or userspace support, interfaces, routes, DNS configuration, service units, and firewall state before making changes. On Linux prefer the distribution’s official `wireguard-tools` package and native kernel support; on macOS or Windows use the official WireGuard application. Ask before administrator access, installing drivers, creating interfaces, enabling services, changing routes, DNS, ports, or firewall rules, importing tunnels, or overwriting configuration. Preserve all existing tunnel files, keys, routes, and service state. Configure only `interface_name`, `listen_port`, `address_range`, and `dns_server` when supplied. Use `secret://provider/WIREGUARD_PRIVATE_KEY` and `secret://provider/WIREGUARD_PRESHARED_KEY` only through protected files or secure runtime injection with strict permissions; never print, echo, log, or expose them in process arguments. Do not bring the interface up until peer endpoints and routing impact are approved. Verify `wg --version`, configuration syntax, file permissions, interface creation in a non-connected or test namespace when possible, and route conflict checks. Rollback disables only the created interface, restores DNS and routes, removes created service files, and preserves user configs. Finish with a structured summary of tooling, interface, addresses, DNS, port, activation state, verification, approvals, and rollback.",
  "tailscale": "Codex, first inspect the OS, architecture, shell, existing Tailscale client, daemon or system extension, network interfaces, current login state, advertised routes, DNS settings, and exit-node configuration. Use Tailscale’s official package or signed application: Homebrew Cask or official macOS app, the official Linux repository and `tailscale` package, and the official Windows installer or `winget` package. Ask before administrator access, installing network extensions, enabling or restarting the daemon, changing routes, DNS, firewall rules, opening a browser, logging in, advertising subnets, or selecting an exit node. Preserve current preferences, node identity, keys, routes, and service state. Configure only `hostname`, `accept_dns`, `advertise_routes`, and `exit_node` when supplied. Use `secret://provider/TS_AUTHKEY` through a non-printing `tailscale up` authentication path; never print, echo, log, or persist it in shell history. Do not authenticate through a browser without approval. Verify client and daemon versions, `tailscale status`, interface state, DNS behavior, and connectivity to an approved peer only. Rollback restores previous preferences, disables routes or exit-node changes made here, and uninstalls only the new client package. Finish with a structured summary of installation, authentication method, hostname, routes, DNS, exit node, verification, approvals, and rollback.",
  "mattermost": "Codex, inspect the OS, architecture, shell, existing Mattermost deployment, Docker or Kubernetes tooling, PostgreSQL database, file storage, configuration, plugins, site URL, and occupied ports. Prefer Mattermost’s official Docker deployment for a new self-hosted instance; use official Linux packages only when an existing managed server layout is already present. Ask before administrator access, installing services, creating databases, running migrations, exposing ports, changing firewall rules, opening a browser, enabling plugins, or overwriting configuration. Preserve messages, users, teams, uploads, database volumes, plugins, and `config.json`. Configure only `server_port`, `site_url`, `default_team`, and `file_storage` when supplied. Inject `secret://provider/MM_SQLSETTINGS_DATASOURCE` and `secret://provider/MM_SERVICESETTINGS_SITEURL` through protected runtime configuration; never print, echo, log, or commit values. Keep the deployment local until URL, TLS, and proxy settings are approved. Verify official image or package identity, database connectivity, migration completion, file-storage permissions, server health, and login-page availability without browser sign-in. Rollback restores configuration, removes only newly created services, and preserves database and uploaded files unless deletion is explicitly authorized. Finish with a structured summary of topology, database, storage, URL, port, verification, approvals, and rollback.",
  "rocket-chat": "Codex, begin by detecting the OS, architecture, shell, Docker availability, existing Rocket.Chat deployment, MongoDB replica-set state, volumes, environment files, reverse proxy, and occupied ports. Prefer Rocket.Chat’s official Docker Compose deployment and supported MongoDB configuration; do not use an unsupported standalone database layout. Ask before administrator access, installing Docker, initializing or changing a MongoDB replica set, starting services, opening ports, changing firewall rules, opening a browser, or overwriting environment files. Preserve channels, users, uploads, database volumes, settings, and integrations. Configure only `server_port`, `site_name`, and `default_language` when supplied. Inject `secret://provider/MONGO_URL`, `secret://provider/MONGO_OPLOG_URL`, and `secret://provider/ROOT_URL` through protected runtime secrets or permission-restricted environment files; never print, echo, log, or commit values. Bind locally until the public URL, TLS, and proxy are approved. Verify official image sources, MongoDB health and replica-set readiness, Rocket.Chat startup, migration completion, HTTP readiness, and the login page without registering users. Rollback restores compose files, removes only newly created containers, and retains MongoDB and uploads unless deletion is authorized. Finish with a structured summary of services, database topology, site settings, port, verification, approvals, and rollback.",
  "element": "Codex, inspect the OS, architecture, shell, existing Element desktop or web installation, configuration files, homeserver settings, caches, encryption stores, and account state before changes. For desktop use Element’s official signed packages on macOS, Linux, or Windows; for self-hosted web use the official `element-hq/element-web` container or release archive. Ask before administrator access, replacing the application, exposing web ports, changing firewall rules, opening a browser, signing in, clearing caches, or overwriting `config.json`. Preserve local encryption keys, session data, trusted-device state, custom branding, and existing homeserver configuration. Configure only `homeserver_url`, `default_server_name`, `theme`, and `brand_name` when supplied. Do not request or handle account passwords or recovery keys. Validate homeserver URLs and well-known discovery without authenticating. Verify package or image identity, application launch or web readiness, configuration parsing, homeserver reachability, theme and branding, and access to the unauthenticated login screen. Never create a Matrix account automatically. Rollback restores config and cached settings, removes only the desktop package or web container created here, and never deletes encryption stores without explicit approval. Finish with a structured summary of deployment mode, homeserver, branding, theme, connectivity, verification, approvals, and rollback.",
  "zulip": "Codex, first inspect the OS, architecture, shell, existing Zulip server deployment, Docker support, PostgreSQL, Redis, RabbitMQ, uploads, configuration, hostname, and occupied ports. Prefer Zulip’s official production installer on a supported Ubuntu server; use the official Docker image only for development or evaluation according to Zulip documentation. Do not pretend macOS or Windows are supported production hosts. Ask before administrator access, installing system services, creating databases, running migrations, exposing ports, changing firewall rules, opening a browser, configuring email, or overwriting deployment files. Preserve realms, messages, users, uploads, database backups, secrets, and configuration. Configure only `external_host`, `server_port`, and `default_language` when supplied. Inject `secret://provider/ZULIP_ADMINISTRATOR`, `secret://provider/POSTGRES_PASSWORD`, and `secret://provider/SECRET_KEY` through protected installer inputs or secret files; never print, echo, log, or commit values. Keep the instance private until DNS, TLS, and outbound email are approved. Verify services, database migrations, queue workers, web health, static assets, and login-page access without creating users. Rollback uses supported uninstall or service removal steps, restores backups, and retains database and uploads unless deletion is authorized. Finish with a structured summary of support status, deployment, hostname, services, language, verification, approvals, and rollback.",
  "home-assistant": "Codex, inspect the OS, architecture, shell, existing Home Assistant installation, configuration directory, integrations, backups, database, USB devices, network discovery, and occupied port 8123. Prefer Home Assistant Container with the official image on a supported Docker host, or Home Assistant OS only when the user explicitly approves dedicating a machine or VM. Do not install Home Assistant Core into the system Python. Ask before administrator access, installing Docker, creating services, granting USB or host-network access, opening ports, changing firewall rules, launching a browser, or overwriting configuration. Preserve `configuration.yaml`, secrets files, automations, dashboards, add-ons, backups, and database data. Configure only `config_directory`, `server_port`, `timezone`, and `unit_system` when supplied. Use `secret://provider/HASS_TOKEN` only through protected runtime integration where needed; never print, echo, log, or commit it. Validate YAML with Home Assistant’s configuration check before restart. Verify image identity, persistent storage, startup logs, HTTP readiness, timezone and unit settings, and configuration validation without onboarding or discovering devices automatically. Rollback restores configuration, stops and removes only created containers, and preserves all data and backups unless deletion is approved. Finish with a structured summary of installation type, storage, networking, settings, verification, approvals, and rollback.",
  "n8n": "Codex, begin by detecting the OS, architecture, shell, Docker or Node.js availability, existing n8n deployment, database, encryption key state, workflow data, credentials, public URL, and occupied ports. Prefer n8n’s official Docker image with a persistent data volume; use the official npm package only inside an existing managed Node environment. Ask before administrator access, installing Docker or Node, starting services, exposing ports, changing firewall rules, opening a browser, running database migrations, enabling community nodes, or overwriting environment files. Preserve workflows, executions, credentials, encryption settings, database volumes, and binary data. Configure only `server_port`, `public_url`, `timezone`, and `database_type` when supplied. Inject `secret://provider/N8N_ENCRYPTION_KEY`, `secret://provider/N8N_BASIC_AUTH_PASSWORD`, and `secret://provider/DB_POSTGRESDB_PASSWORD` through protected runtime secrets; never print, echo, log, or commit values. Keep the editor localhost-bound unless authentication, TLS, and proxy settings are approved. Verify image or package identity, database connectivity, migration completion, health endpoint, timezone behavior, and creation plus deletion of a disposable manual workflow without external credentials. Rollback restores manifests, removes only services created here, and retains workflows and database data unless deletion is authorized. Finish with a structured summary of runtime, database, URL, timezone, security, verification, approvals, and rollback.",
  "immich": "Codex, inspect the OS, architecture, shell, Docker and Compose availability, existing Immich deployment, upload library, PostgreSQL data, machine-learning cache, environment files, backups, and occupied ports. Prefer Immich’s official Docker Compose release files and documented upgrade procedure. Ask before administrator access, installing or starting Docker, creating services, running database migrations, binding ports, changing firewall rules, opening a browser, scanning photo libraries, or overwriting environment files. Preserve originals, thumbnails, metadata, albums, database volumes, user accounts, and backups. Configure only `upload_location`, `server_port`, `timezone`, and `machine_learning_enabled` when supplied. Inject `secret://provider/DB_PASSWORD` through a protected environment or secret file with restrictive permissions; never print, echo, log, or commit it. Validate upload-path ownership and free space before startup. Do not import or scan user media during verification. Verify official image sources, container health, database migration completion, storage permissions, machine-learning service state when enabled, API readiness, and login-page availability. Rollback restores compose and environment backups, removes only newly created containers, and retains all photos and database volumes unless deletion is explicitly authorized. Finish with a structured summary of release source, storage, database, ML state, port, timezone, verification, approvals, and rollback.",
  "uptime-kuma": "Codex, first inspect the OS, architecture, shell, Docker or Node.js availability, existing Uptime Kuma deployment, data directory, monitors, notification settings, reverse proxy, and occupied port 3001. Prefer the official `louislam/uptime-kuma` Docker image with a persistent `/app/data` volume; use the documented Node.js installation only when containers are unsuitable. Ask before administrator access, installing container tooling, starting a service, exposing ports, changing firewall rules, opening a browser, replacing containers, or overwriting data. Preserve monitors, status pages, notification integrations, certificates, database files, and backups. Configure only `server_port`, `data_directory`, `public_url`, and `timezone` when supplied. Bind to localhost unless a reverse proxy, TLS, and authentication plan are approved. Do not configure third-party notification credentials automatically. Verify image or package identity, persistent mount, startup health, HTTP readiness, timezone display, and creation plus deletion of a disposable localhost monitor. Avoid probing external systems without approval. Rollback stops and removes only created containers or services, restores manifests, and retains the data directory unless deletion is explicitly authorized. Finish with a structured summary of deployment, storage, URL, timezone, monitor test, verification, approvals, and rollback.",
  "syncthing": "Codex, inspect the OS, architecture, shell, existing Syncthing binary, service state, device ID, configuration, folders, database, GUI binding, discovery settings, and listening ports before changes. On macOS prefer Homebrew `syncthing` or the official GUI wrapper; on Linux use the distribution package or Syncthing’s official repository; on Windows prefer the official package or supported `winget` entry. Ask before administrator access, enabling startup services, opening ports, changing firewall rules, exposing the GUI, adding devices, sharing folders, or overwriting configuration. Preserve device identity, certificates, folder indexes, ignore files, versioning, and existing peers. Configure only `sync_directory`, `device_name`, `gui_address`, and `listen_address` when supplied. Use `secret://provider/STGUIAPIKEY` only through protected configuration handling; never print, echo, log, or expose it in commands. Keep the GUI on localhost and do not add remote devices automatically. Verify `syncthing --version`, configuration validity, service health, GUI readiness, directory permissions, and synchronization between two disposable local folders using a temporary isolated home directory. Rollback restores config and prior service state, removes only test folders and the installed package, and preserves real indexes and identity. Finish with a structured summary of installation, identity, paths, bindings, service state, verification, approvals, and rollback.",
  "rufus": "Codex, first inspect the Windows version, architecture, shell, existing Rufus executable, package ownership, connected removable drives, current drive letters, and any saved Rufus settings. Rufus is Windows-only; do not attempt installation on macOS or Linux. Prefer the official signed Rufus executable from `rufus.ie` or the supported `winget install Rufus.Rufus` package. Ask before administrator access, downloading an image, changing update settings, writing to any USB device, modifying partitions, assigning drive letters, or overwriting files. Preserve the existing executable, configuration, logs, and all connected media. Configure only `default_device`, `partition_scheme`, `filesystem`, and `image_path` when supplied. Treat `default_device` as a selection preference only; never start a write operation without separately displaying the device model, capacity, current partitions, image path, partition scheme, and filesystem and receiving explicit approval. Verify the digital signature, executable version, image readability and checksum when available, and compatibility of the selected image and target settings. Do not perform a destructive test write. Rollback removes only the installed executable or package and restores preferences; no disk changes should occur without approval. Finish with a structured summary of support status, installer source, detected devices, selected settings, image validation, pending destructive approval, verification, and rollback."
}
